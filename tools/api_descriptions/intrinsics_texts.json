{
  "gb_intrinsics_role": {
    "ja": "この節はKITAQGBのコード生成器が直接処理する組み込み関数をまとめています。`intrinsics`は説明書内の分類名で、GB用の`intrinsics.h`という共通ヘッダーを取り込む指示ではありません。各項目の完成サンプルにあるヘッダーと初期化処理を使ってください。組み込み関数の本体をゲーム側で定義する必要はありません。",
    "en": "This section groups intrinsics handled directly by the KITAQGB code generator. `intrinsics` is a documentation category, not an instruction to include a shared GB header named `intrinsics.h`. Use the headers and initialization shown in each complete example. You do not need to define the intrinsic's implementation in your game."
  },
  "gb_intrinsics_values": {
    "ja": "組み込み関数には、ビルド時の情報を使うものと、ゲーム実行中に機器やメモリを操作するものがあります。たとえば`__bankof(symbol)`は名前で指定したデータ・関数の配置バンクを求め、`__bankswitch(bank)`は実行時にROMの見える範囲を切り替えます。バンク番号、CPUアドレス、ポインターを区別して引数を渡してください。",
    "en": "Some intrinsics use build-time information; others access hardware or memory while the game runs. For example, `__bankof(symbol)` resolves the assigned bank of a named object or function, while `__bankswitch(bank)` changes the visible ROM mapping at runtime. Keep bank numbers, CPU addresses and pointers distinct when supplying arguments."
  },
  "gb_intrinsics_context": {
    "ja": "`__`で始まる名前だけでは、範囲検査、VRAMアクセスの待機、割り込み中の安全性は保証されません。たとえば`__vram_memcpy_unsafe`では、呼び出し側がVRAMへ書ける期間を確保します。個別説明の引数の単位・有効範囲・バンク配置・割り込み条件を確認し、用途に合った命令を選んでください。",
    "en": "A leading `__` does not guarantee bounds checking, VRAM access waits or safe use from an interrupt. For example, the caller of `__vram_memcpy_unsafe` must provide a writable VRAM period. Check each entry's units, valid ranges, bank mapping and interrupt requirements before choosing an operation."
  }
}

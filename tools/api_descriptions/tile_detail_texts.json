{
  "tile_xy": ["Coordinates in 8×8 tiles, each from 0 to 31. Keep the complete write inside the 32×32 map; coordinates are not clipped.", "8×8タイル単位の座標で、それぞれ0～31です。書き込み範囲全体を32×32のマップ内に収めてください。座標の切り詰めは行いません。", "8×8 타일 단위의 좌표이며 각각 0~31입니다. 쓰기 범위 전체가 32×32 맵 안에 들어가야 합니다. 좌표를 자동으로 잘라 주지 않습니다.", "以 8×8 图块为单位的坐标，各为 0～31。整个写入范围必须位于 32×32 地图内；不会自动裁剪坐标。", "以 8×8 圖塊為單位的座標，各為 0～31。整個寫入範圍必須位於 32×32 地圖內；不會自動裁切座標。", "Coordenadas en tiles de 8×8, de 0 a 31. Toda la escritura debe quedar dentro del mapa de 32×32; no se recortan las coordenadas.", "Coordenadas em tiles de 8×8, de 0 a 31. Toda a gravação deve ficar dentro do mapa de 32×32; não há recorte automático das coordenadas.", "Coordonnées en tuiles de 8×8, de 0 à 31 chacune. Toute l’écriture doit rester dans la carte de 32×32 ; les coordonnées ne sont pas bornées automatiquement.", "Koordinaten in 8×8-Tiles, jeweils von 0 bis 31. Der gesamte Schreibbereich muss in der 32×32-Karte liegen; Koordinaten werden nicht automatisch begrenzt."],
  "tile_id": ["Tile number, 0–255. LCDC bit 4 determines how the number addresses tile-image data. It is not a VRAM byte address.", "0～255のタイル番号です。番号から画像データを参照する方式はLCDCのビット4で決まります。VRAMのバイトアドレスではありません。", "0~255의 타일 번호입니다. 번호가 타일 이미지 데이터를 가리키는 방식은 LCDC 비트 4로 결정됩니다. VRAM 바이트 주소가 아닙니다.", "0～255 的图块编号。LCDC 位 4 决定编号如何寻址图块图像数据；这不是 VRAM 字节地址。", "0～255 的圖塊編號。LCDC 位元 4 決定編號如何定址圖塊影像資料；這不是 VRAM 位元組位址。", "Número de tile, de 0 a 255. El bit 4 de LCDC determina cómo se localiza su imagen. No es una dirección de byte de VRAM.", "Número do tile, de 0 a 255. O bit 4 de LCDC determina como localizar a imagem. Não é um endereço de byte da VRAM.", "Numéro de tuile, de 0 à 255. Le bit 4 de LCDC détermine l’adressage de son image. Ce n’est pas une adresse d’octet en VRAM.", "Tile-Nummer von 0 bis 255. LCDC-Bit 4 bestimmt, wie sie die Tile-Bilddaten adressiert. Sie ist keine VRAM-Byteadresse."],
  "tile_attribute": ["CGB attribute byte: bits 0–2 select palette 0–7; bit 3 selects tile-data bank; bits 5 and 6 flip horizontally and vertically; bit 7 sets BG/window priority. Keep unused bit 4 zero.", "CGBの属性バイトです。ビット0～2はパレット0～7、ビット3は画像データのバンク、ビット5・6は左右・上下反転、ビット7はBG・ウィンドウの優先度です。未使用のビット4は0にしてください。", "CGB 속성 바이트입니다. 비트 0~2는 팔레트 0~7, 비트 3은 이미지 데이터 뱅크, 비트 5·6은 좌우·상하 반전, 비트 7은 BG·윈도 우선순위입니다. 사용하지 않는 비트 4는 0으로 두세요.", "CGB 属性字节：位 0～2 选择调色板 0～7，位 3 选择图像数据 bank，位 5、6 分别控制水平和垂直翻转，位 7 设置背景／窗口优先级。未使用的位 4 应为 0。", "CGB 屬性位元組：位元 0～2 選擇調色盤 0～7，位元 3 選擇影像資料 bank，位元 5、6 分別控制水平及垂直翻轉，位元 7 設定背景／視窗優先順序。未使用的位元 4 應為 0。", "Byte de atributos CGB: bits 0–2, paleta 0–7; bit 3, banco de imagen; bits 5 y 6, inversión horizontal y vertical; bit 7, prioridad del fondo o ventana. Deja el bit 4 sin usar en cero.", "Byte de atributos do CGB: bits 0–2, paleta 0–7; bit 3, banco da imagem; bits 5 e 6, inversão horizontal e vertical; bit 7, prioridade do fundo ou janela. Mantenha o bit 4, não utilizado, em zero.", "Octet d’attributs CGB : bits 0–2, palette 0–7 ; bit 3, banque d’image ; bits 5 et 6, retournement horizontal et vertical ; bit 7, priorité du fond ou de la fenêtre. Laissez à zéro le bit 4 inutilisé.", "CGB-Attributbyte: Bits 0–2 wählen Palette 0–7, Bit 3 die Tile-Datenbank, Bits 5 und 6 die horizontale und vertikale Spiegelung, Bit 7 die BG-/Fensterpriorität. Das unbenutzte Bit 4 bleibt null."],
  "tile_dest": ["First VRAM byte address. For a map, use an address in `0x9800–0x9FFF` and keep the full transfer within the intended map.", "先頭のVRAMバイトアドレスです。マップには `0x9800～0x9FFF` の範囲を使い、転送全体が目的のマップ内に収まるようにしてください。", "첫 VRAM 바이트 주소입니다. 맵에는 `0x9800~0x9FFF` 범위를 사용하고 전송 전체가 해당 맵 안에 들어가도록 하세요.", "起始 VRAM 字节地址。地图使用 `0x9800–0x9FFF` 范围，整个传输应位于目标地图内。", "起始 VRAM 位元組位址。地圖使用 `0x9800–0x9FFF` 範圍，整段傳輸應位於目標地圖內。", "Dirección del primer byte en VRAM. Para un mapa, usa `0x9800–0x9FFF` y mantén toda la transferencia dentro del mapa elegido.", "Endereço do primeiro byte na VRAM. Para um mapa, use `0x9800–0x9FFF` e mantenha toda a transferência dentro do mapa desejado.", "Adresse du premier octet en VRAM. Pour une carte, utilisez `0x9800–0x9FFF` et gardez tout le transfert dans la carte visée.", "VRAM-Byteadresse des Anfangs. Für Karten gilt `0x9800–0x9FFF`; der gesamte Transfer muss innerhalb der gewünschten Karte bleiben."],
  "tile_count": ["Number of bytes, 0–255. Zero copies nothing. Supply at least this many readable source bytes.", "バイト数で、0～255です。0なら何も転送しません。転送数以上の読み取り可能なデータを用意してください。", "바이트 수이며 0~255입니다. 0이면 복사하지 않습니다. 이 수만큼 읽을 수 있는 원본 데이터를 준비하세요.", "字节数，范围 0～255。为 0 时不复制；源数据至少要有这么多可读字节。", "位元組數，範圍 0～255。為 0 時不複製；來源資料至少要有這麼多可讀位元組。", "Cantidad de bytes, de 0 a 255. Cero no copia nada. El origen debe contener al menos esa cantidad de bytes legibles.", "Quantidade de bytes, de 0 a 255. Zero não copia nada. A origem deve conter pelo menos essa quantidade de bytes legíveis.", "Nombre d’octets, de 0 à 255. Zéro ne copie rien. La source doit contenir au moins autant d’octets lisibles.", "Byteanzahl von 0 bis 255. Null kopiert nichts. Die Quelle muss mindestens so viele lesbare Bytes enthalten."],
  "tile_sources": ["Packed source bytes in RAM or a visible ROM bank. A tile stream contains tile numbers; an attribute stream contains CGB attribute bytes. The routine copies now and does not retain the pointer.", "RAMまたは参照可能なROMバンク上に、隙間なく並べたデータです。タイル列にはタイル番号、属性列にはCGB属性を格納します。その場でコピーし、ポインタは保存しません。", "RAM 또는 현재 읽을 수 있는 ROM 뱅크에 연속으로 놓인 데이터입니다. 타일 배열에는 타일 번호, 속성 배열에는 CGB 속성을 넣습니다. 즉시 복사하며 포인터를 보관하지 않습니다.", "位于 RAM 或当前可见 ROM bank 中的连续字节。图块流保存图块编号，属性流保存 CGB 属性字节。函数立即复制，不保留指针。", "位於 RAM 或目前可見 ROM bank 中的連續位元組。圖塊資料流存放圖塊編號，屬性資料流存放 CGB 屬性位元組。函式立即複製，不保留指標。", "Bytes contiguos en RAM o en un banco ROM visible. La secuencia de tiles contiene números; la de atributos, bytes de atributos CGB. La copia es inmediata y no conserva el puntero.", "Bytes contíguos na RAM ou em um banco ROM visível. A sequência de tiles contém números; a de atributos, bytes de atributos do CGB. A cópia é imediata e não guarda o ponteiro.", "Octets contigus en RAM ou dans une banque ROM visible. Le flux de tuiles contient des numéros ; celui d’attributs contient les attributs CGB. La copie est immédiate et le pointeur n’est pas conservé.", "Lückenlose Bytes im RAM oder einer sichtbaren ROM-Bank. Der Tile-Strom enthält Tile-Nummern, der Attributstrom CGB-Attribute. Die Kopie erfolgt sofort; der Zeiger wird nicht gespeichert."],
  "tile_rect_fill": ["Fill a `w` by `h` rectangle in the LCDC-selected BG map with one tile number. Rows are 32 bytes apart; this repeats a tile, rather than copying an image array.", "LCDCで選択したBGマップの幅 `w`、高さ `h` の矩形を、同じタイル番号で埋めます。行の間隔は32バイトです。画像の配列をコピーする処理ではなく、同じタイルを繰り返し配置します。", "LCDC로 선택한 BG 맵에서 너비 `w`, 높이 `h`의 사각형을 같은 타일 번호로 채웁니다. 행 간격은 32바이트입니다. 이미지 배열을 복사하지 않고 같은 타일을 반복 배치합니다.", "用同一个图块编号填充 LCDC 所选背景地图中宽 `w`、高 `h` 的矩形。行间距为 32 字节；这是重复铺放同一图块，而非复制图像数组。", "以同一圖塊編號填滿 LCDC 所選背景地圖中寬 `w`、高 `h` 的矩形。列間距為 32 位元組；這是重複放置同一圖塊，而非複製影像陣列。", "Rellena un rectángulo de `w` por `h` en el mapa BG seleccionado por LCDC con un solo número de tile. Las filas están separadas por 32 bytes; repite un tile, no copia una matriz de imágenes.", "Preenche um retângulo de `w` por `h` no mapa BG selecionado pelo LCDC com um único número de tile. As linhas têm passo de 32 bytes; repete um tile, sem copiar uma matriz de imagens.", "Remplit un rectangle de `w` sur `h` dans la carte BG sélectionnée par LCDC avec un seul numéro de tuile. Le pas des lignes est de 32 octets ; la routine répète une tuile et ne copie pas un tableau d’images.", "Füllt ein Rechteck von `w` mal `h` in der durch LCDC gewählten BG-Karte mit einer Tile-Nummer. Der Zeilenabstand beträgt 32 Bytes; es wird ein Tile wiederholt, kein Bildarray kopiert."],
  "tile_rect_size": ["Width and height in 8×8 tiles. Keep `x+w <= 32` and `y+h <= 32`. A zero dimension performs no write.", "8×8タイル単位の幅と高さです。`x+w <= 32`、`y+h <= 32` に収めてください。いずれかが0なら書き込みません。", "8×8 타일 단위의 너비와 높이입니다. `x+w <= 32`, `y+h <= 32`를 지키세요. 어느 하나가 0이면 쓰지 않습니다.", "以 8×8 图块为单位的宽和高。须满足 `x+w <= 32`、`y+h <= 32`。任一维为 0 时不写入。", "以 8×8 圖塊為單位的寬與高。須滿足 `x+w <= 32`、`y+h <= 32`。任一維為 0 時不寫入。", "Ancho y alto en tiles de 8×8. Mantén `x+w <= 32` e `y+h <= 32`. Si alguna dimensión es cero, no se escribe nada.", "Largura e altura em tiles de 8×8. Mantenha `x+w <= 32` e `y+h <= 32`. Se uma dimensão for zero, nada será gravado.", "Largeur et hauteur en tuiles de 8×8. Respectez `x+w <= 32` et `y+h <= 32`. Une dimension nulle n’effectue aucune écriture.", "Breite und Höhe in 8×8-Tiles. Es müssen `x+w <= 32` und `y+h <= 32` gelten. Bei einer Nulldimension wird nichts geschrieben."],
  "tile_row": ["Copy `len` successive tile numbers from `src` horizontally, starting at `(x,y)` in the LCDC-selected BG map. Source and destination advance by one byte per tile.", "`src` の連続するタイル番号を、LCDCで選んだBGマップの `(x,y)` から右へ `len` 個コピーします。コピー元・先は1タイルごとに1バイト進みます。", "`src`의 연속된 타일 번호 `len`개를 LCDC로 선택한 BG 맵의 `(x,y)`부터 오른쪽으로 복사합니다. 원본과 대상 주소가 타일마다 1바이트씩 증가합니다.", "将 `src` 中连续的 `len` 个图块编号，从 LCDC 所选背景地图的 `(x,y)` 起向右复制。每复制一个图块，源和目标地址各增加一字节。", "將 `src` 中連續的 `len` 個圖塊編號，從 LCDC 所選背景地圖的 `(x,y)` 起向右複製。每複製一個圖塊，來源與目標位址各增加一位元組。", "Copia horizontalmente `len` números de tile consecutivos de `src`, desde `(x,y)` en el mapa BG seleccionado por LCDC. Ambos punteros avanzan un byte por tile.", "Copia horizontalmente `len` números de tile consecutivos de `src`, a partir de `(x,y)` no mapa BG selecionado pelo LCDC. Origem e destino avançam um byte por tile.", "Copie horizontalement `len` numéros de tuile consécutifs de `src`, à partir de `(x,y)` dans la carte BG sélectionnée par LCDC. Source et destination avancent d’un octet par tuile.", "Kopiert `len` aufeinanderfolgende Tile-Nummern aus `src` waagerecht ab `(x,y)` in die durch LCDC gewählte BG-Karte. Quell- und Zieladresse steigen pro Tile um ein Byte."],
  "tile_col": ["Copy `len` successive tile numbers from `src` vertically, starting at `(x,y)` in the LCDC-selected BG map. The source advances one byte and the destination 32 bytes per tile.", "`src` の連続するタイル番号を、LCDCで選んだBGマップの `(x,y)` から下へ `len` 個コピーします。コピー元は1バイト、コピー先は32バイトずつ進みます。", "`src`의 연속된 타일 번호 `len`개를 LCDC로 선택한 BG 맵의 `(x,y)`부터 아래로 복사합니다. 원본은 1바이트, 대상은 32바이트씩 증가합니다.", "将 `src` 中连续的 `len` 个图块编号，从 LCDC 所选背景地图的 `(x,y)` 起向下复制。源地址每次增加一字节，目标地址增加 32 字节。", "將 `src` 中連續的 `len` 個圖塊編號，從 LCDC 所選背景地圖的 `(x,y)` 起向下複製。來源位址每次增加一位元組，目標位址增加 32 位元組。", "Copia verticalmente `len` números de tile consecutivos de `src`, desde `(x,y)` en el mapa BG seleccionado por LCDC. El origen avanza un byte y el destino 32 por tile.", "Copia verticalmente `len` números de tile consecutivos de `src`, a partir de `(x,y)` no mapa BG selecionado pelo LCDC. A origem avança um byte e o destino, 32 por tile.", "Copie verticalement `len` numéros de tuile consécutifs de `src`, à partir de `(x,y)` dans la carte BG sélectionnée par LCDC. La source avance d’un octet et la destination de 32 par tuile.", "Kopiert `len` aufeinanderfolgende Tile-Nummern aus `src` senkrecht ab `(x,y)` in die durch LCDC gewählte BG-Karte. Die Quelle steigt pro Tile um ein Byte, das Ziel um 32 Bytes."],
  "tile_map_rect": ["Copy a tightly packed `w*h` array of tile numbers to a rectangle at `(x,y)` in the map selected by `base`. Source rows have `w` bytes; destination rows have a 32-byte stride.", "隙間なく並んだ `w*h` 個のタイル番号を、`base` で指定したマップの `(x,y)` から矩形としてコピーします。コピー元は1行 `w` バイト、コピー先の行間隔は32バイトです。", "빈틈없이 배열된 `w*h`개의 타일 번호를 `base`로 지정한 맵의 `(x,y)`에 사각형으로 복사합니다. 원본 행은 `w`바이트이며 대상 행 간격은 32바이트입니다.", "将紧密排列的 `w*h` 个图块编号复制到 `base` 指定地图中以 `(x,y)` 为起点的矩形。源数据每行 `w` 字节，目标行间距为 32 字节。", "將緊密排列的 `w*h` 個圖塊編號複製到 `base` 指定地圖中以 `(x,y)` 為起點的矩形。來源資料每列 `w` 位元組，目標列間距為 32 位元組。", "Copia una matriz compacta de `w*h` números de tile a un rectángulo en `(x,y)` del mapa elegido por `base`. Las filas de origen tienen `w` bytes; el destino usa un paso de 32 bytes.", "Copia uma matriz compacta de `w*h` números de tile para um retângulo em `(x,y)` no mapa escolhido por `base`. As linhas da origem têm `w` bytes; o destino usa passo de 32 bytes.", "Copie un tableau compact de `w*h` numéros de tuile dans un rectangle à `(x,y)` de la carte choisie par `base`. Les lignes sources ont `w` octets ; le pas des lignes de destination est de 32 octets.", "Kopiert ein lückenloses Array mit `w*h` Tile-Nummern in ein Rechteck ab `(x,y)` der mit `base` gewählten Karte. Quellzeilen enthalten `w` Bytes; Zielzeilen haben 32 Bytes Abstand."],
  "tile_quad": ["Write a 16×16-pixel cell into a RAM tile-map buffer: the four 8×8 tile numbers are `tile,tile+1` on top and `tile+2,tile+3` below. Values wrap at 256. This does not transfer anything to VRAM.", "RAM上のタイルマップバッファに16×16ピクセルのセルを書き込みます。8×8タイル4枚の番号は上段が `tile,tile+1`、下段が `tile+2,tile+3` で、256で折り返します。VRAMへの転送は行いません。", "RAM 타일 맵 버퍼에 16×16픽셀 셀을 씁니다. 8×8 타일 네 개의 번호는 위쪽이 `tile,tile+1`, 아래쪽이 `tile+2,tile+3`이며 256에서 순환합니다. VRAM 전송은 하지 않습니다.", "在 RAM 图块地图缓冲区写入一个 16×16 像素单元。四个 8×8 图块编号上排为 `tile,tile+1`，下排为 `tile+2,tile+3`，超过 255 后回绕。不向 VRAM 传输。", "在 RAM 圖塊地圖緩衝區寫入一個 16×16 像素單元。四個 8×8 圖塊編號上排為 `tile,tile+1`，下排為 `tile+2,tile+3`，超過 255 後回繞。不傳輸至 VRAM。", "Escribe una celda de 16×16 píxeles en un búfer de mapa en RAM: arriba, `tile,tile+1`; abajo, `tile+2,tile+3`. Los valores vuelven a cero al llegar a 256. No transfiere nada a VRAM.", "Grava uma célula de 16×16 pixels em um buffer de mapa na RAM: acima, `tile,tile+1`; abaixo, `tile+2,tile+3`. Os valores retornam a zero ao atingir 256. Não transfere dados para a VRAM.", "Écrit une cellule de 16×16 pixels dans un tampon de carte en RAM : `tile,tile+1` en haut, `tile+2,tile+3` en bas. Les valeurs rebouclent à 256. Aucun transfert vers la VRAM n’est effectué.", "Schreibt eine 16×16-Pixel-Zelle in einen Tile-Kartenpuffer im RAM: oben `tile,tile+1`, unten `tile+2,tile+3`. Die Werte laufen bei 256 über. Es erfolgt kein VRAM-Transfer."],
  "tile_quad_attr": ["Also write the same `attr` byte to all four corresponding cells in the separate attribute buffer. Both buffers are ordinary RAM, so this preparation also runs on DMG.", "別の属性バッファの対応する4セルにも、同じ `attr` を書き込みます。どちらも通常のRAMなので、DMG上でもバッファの準備処理は行われます。", "별도의 속성 버퍼에서 대응하는 네 셀 모두에 같은 `attr` 바이트를 씁니다. 두 버퍼 모두 일반 RAM이므로 DMG에서도 이 준비 작업이 실행됩니다.", "还会在独立属性缓冲区的对应四个单元写入相同的 `attr`。两个缓冲区均为普通 RAM，因此 DMG 也会执行此准备操作。", "也會在獨立屬性緩衝區的對應四個單元寫入相同的 `attr`。兩個緩衝區均為一般 RAM，因此 DMG 也會執行此準備操作。", "Escribe también el mismo byte `attr` en las cuatro celdas del búfer de atributos separado. Ambos búferes son RAM normal, por lo que la preparación también se ejecuta en DMG.", "Também grava o mesmo byte `attr` nas quatro células correspondentes do buffer separado de atributos. Ambos são RAM comum; portanto, essa preparação também é executada no DMG.", "Écrit aussi le même octet `attr` dans les quatre cases correspondantes du tampon d’attributs séparé. Les deux tampons étant en RAM ordinaire, cette préparation s’exécute aussi sur DMG.", "Schreibt außerdem dasselbe `attr`-Byte in die vier entsprechenden Zellen des separaten Attributpuffers. Beide Puffer liegen im normalen RAM; die Vorbereitung läuft daher auch auf DMG."],
  "tile_quad_buffer": ["A RAM buffer with 32 bytes per tile row. A full 32×32 map needs 1024 bytes. The upper-left byte is at `64*y + 2*x`; the four offsets are `0,1,32,33` from there.", "タイル1行あたり32バイトのRAMバッファです。32×32のマップ全体なら1024バイト必要です。左上は `64*y + 2*x` バイト目で、そこから `0,1,32,33` の位置に書き込みます。", "타일 행당 32바이트인 RAM 버퍼입니다. 전체 32×32 맵은 1024바이트가 필요합니다. 왼쪽 위 위치는 `64*y + 2*x`이며 그곳에서 `0,1,32,33` 오프셋에 씁니다.", "每图块行 32 字节的 RAM 缓冲区。完整 32×32 地图需要 1024 字节。左上角偏移为 `64*y + 2*x`，四个写入位置相对此处为 `0,1,32,33`。", "每圖塊列 32 位元組的 RAM 緩衝區。完整 32×32 地圖需要 1024 位元組。左上角偏移為 `64*y + 2*x`，四個寫入位置相对此處為 `0,1,32,33`。", "Búfer RAM con 32 bytes por fila de tiles. Un mapa completo de 32×32 necesita 1024 bytes. La esquina superior izquierda está en `64*y + 2*x`; los cuatro desplazamientos son `0,1,32,33` desde allí.", "Buffer RAM com 32 bytes por linha de tiles. Um mapa completo de 32×32 precisa de 1024 bytes. O canto superior esquerdo está em `64*y + 2*x`; os quatro deslocamentos a partir dali são `0,1,32,33`.", "Tampon RAM avec 32 octets par ligne de tuiles. Une carte complète de 32×32 demande 1024 octets. Le coin supérieur gauche est à `64*y + 2*x` ; les quatre décalages relatifs sont `0,1,32,33`.", "RAM-Puffer mit 32 Bytes pro Tile-Zeile. Eine vollständige 32×32-Karte benötigt 1024 Bytes. Oben links liegt bei `64*y + 2*x`; die vier relativen Offsets sind `0,1,32,33`."],
  "tile_quad_xy": ["Coordinates in 16×16-pixel cells, each from 0 to 15 for a full 32×32-tile buffer. These differ from the 8×8-tile coordinates used by scalar setters.", "16×16ピクセルのセル単位です。32×32タイルのバッファ全体では、それぞれ0～15です。1タイルを書き込む関数の8×8タイル座標とは単位が異なります。", "16×16픽셀 셀 단위 좌표입니다. 전체 32×32 타일 버퍼에서는 각각 0~15입니다. 단일 타일 쓰기 함수의 8×8 타일 좌표와 단위가 다릅니다.", "以 16×16 像素单元为单位的坐标；完整 32×32 图块缓冲区中各为 0～15。与单图块写入函数使用的 8×8 图块坐标不同。", "以 16×16 像素單元為單位的座標；完整 32×32 圖塊緩衝區中各為 0～15。與單圖塊寫入函式使用的 8×8 圖塊座標不同。", "Coordenadas en celdas de 16×16 píxeles, de 0 a 15 en un búfer completo de 32×32 tiles. No son las coordenadas de tiles de 8×8 de las escrituras individuales.", "Coordenadas em células de 16×16 pixels, de 0 a 15 em um buffer completo de 32×32 tiles. Diferem das coordenadas em tiles de 8×8 usadas nas gravações individuais.", "Coordonnées en cellules de 16×16 pixels, de 0 à 15 pour un tampon complet de 32×32 tuiles. Leur unité diffère des coordonnées en tuiles de 8×8 des écritures unitaires.", "Koordinaten in 16×16-Pixel-Zellen, jeweils von 0 bis 15 bei einem vollständigen 32×32-Tile-Puffer. Sie unterscheiden sich von den 8×8-Tile-Koordinaten der Einzelzugriffe."],
  "tile_quad_flush": ["Transfer two adjacent tile rows from the RAM buffer to the LCDC-selected BG map. The source offset is `64*y + x`; `count` bytes are copied from that row and from the row 32 bytes below it.", "RAMバッファから隣接するタイル2行を、LCDCで選んだBGマップへ転送します。コピー元は `64*y + x` バイト目で、その行と32バイト下の行から、それぞれ `count` バイトコピーします。", "RAM 버퍼의 인접한 타일 두 행을 LCDC로 선택한 BG 맵으로 전송합니다. 원본 오프셋은 `64*y + x`이며 그 행과 32바이트 아래 행에서 각각 `count`바이트를 복사합니다.", "将 RAM 缓冲区中相邻的两行图块传输到 LCDC 所选背景地图。源偏移为 `64*y + x`，从该行及其下方相隔 32 字节的一行各复制 `count` 字节。", "將 RAM 緩衝區中相鄰的兩列圖塊傳輸到 LCDC 所選背景地圖。來源偏移為 `64*y + x`，從該列及其下方相隔 32 位元組的一列各複製 `count` 位元組。", "Transfiere dos filas contiguas de tiles del búfer RAM al mapa BG seleccionado por LCDC. El origen está en `64*y + x`; copia `count` bytes de esa fila y de la situada 32 bytes después.", "Transfere duas linhas adjacentes de tiles do buffer RAM para o mapa BG selecionado pelo LCDC. A origem está em `64*y + x`; copia `count` bytes dessa linha e da linha 32 bytes abaixo.", "Transfère deux lignes de tuiles adjacentes du tampon RAM vers la carte BG sélectionnée par LCDC. Le décalage source est `64*y + x` ; `count` octets sont copiés depuis cette ligne et celle située 32 octets plus loin.", "Überträgt zwei benachbarte Tile-Zeilen aus dem RAM-Puffer in die durch LCDC gewählte BG-Karte. Der Quelloffset ist `64*y + x`; aus dieser und der 32 Bytes tiefer liegenden Zeile werden jeweils `count` Bytes kopiert."],
  "tile_quad_flush_xy": ["Argument order is `y,x,count`. `y` is a 16-pixel cell row (0–15); `x` and `count` are 8-pixel tile units. Keep `x+count <= 32`. For one cell at `(2,3)`, use `y=3,x=4,count=2`.", "引数の順序は `y,x,count` です。`y` は16ピクセルのセル行（0～15）、`x` と `count` は8ピクセルのタイル単位です。`x+count <= 32` に収めてください。セル `(2,3)` の1個分なら `y=3,x=4,count=2` です。", "인수 순서는 `y,x,count`입니다. `y`는 16픽셀 셀 행(0~15), `x`와 `count`는 8픽셀 타일 단위입니다. `x+count <= 32`를 지키세요. 셀 `(2,3)` 하나는 `y=3,x=4,count=2`로 전송합니다.", "参数顺序为 `y,x,count`。`y` 是 16 像素单元行（0～15），`x` 和 `count` 以 8 像素图块计。须满足 `x+count <= 32`。传输单元 `(2,3)` 时使用 `y=3,x=4,count=2`。", "參數順序為 `y,x,count`。`y` 是 16 像素單元列（0～15），`x` 與 `count` 以 8 像素圖塊計。須滿足 `x+count <= 32`。傳輸單元 `(2,3)` 時使用 `y=3,x=4,count=2`。", "El orden es `y,x,count`. `y` es una fila de celdas de 16 píxeles (0–15); `x` y `count` se miden en tiles de 8 píxeles. Mantén `x+count <= 32`. Para una celda en `(2,3)`, usa `y=3,x=4,count=2`.", "A ordem é `y,x,count`. `y` é uma linha de células de 16 pixels (0–15); `x` e `count` são medidos em tiles de 8 pixels. Mantenha `x+count <= 32`. Para uma célula em `(2,3)`, use `y=3,x=4,count=2`.", "L’ordre est `y,x,count`. `y` est une ligne de cellules de 16 pixels (0–15) ; `x` et `count` sont en tuiles de 8 pixels. Respectez `x+count <= 32`. Pour une cellule en `(2,3)`, utilisez `y=3,x=4,count=2`.", "Die Reihenfolge lautet `y,x,count`. `y` bezeichnet eine 16-Pixel-Zellzeile (0–15), `x` und `count` zählen 8-Pixel-Tiles. Es muss `x+count <= 32` gelten. Für eine Zelle bei `(2,3)` verwenden Sie `y=3,x=4,count=2`."],
  "tile_example_setup": ["The example uses `gb_common.h`: `m_init()` loads the supplied ASCII font with character codes as tile numbers. `m_wait(); M_LCDC=0;` stops the LCD during VBlank before unsafe transfers. The final LCDC value selects which map is visible.", "使用例は `gb_common.h` を使います。`m_init()` は付属のASCIIフォントを文字コードと同じタイル番号に読み込みます。`m_wait(); M_LCDC=0;` はVBlank中にLCDを停止し、待機なしの転送を可能にします。最後のLCDC値で表示するマップを選びます。", "예제는 `gb_common.h`를 사용합니다. `m_init()`은 제공된 ASCII 폰트를 문자 코드와 같은 타일 번호로 불러옵니다. `m_wait(); M_LCDC=0;`은 VBlank 중 LCD를 꺼 대기 없는 전송을 준비합니다. 마지막 LCDC 값으로 표시할 맵을 선택합니다.", "示例使用 `gb_common.h`。`m_init()` 将附带 ASCII 字体加载到与字符编码相同的图块编号。`m_wait(); M_LCDC=0;` 在 VBlank 中关闭 LCD，以便进行无需等待的传输。最后的 LCDC 值选择显示地图。", "範例使用 `gb_common.h`。`m_init()` 將附帶的 ASCII 字型載入與字元編碼相同的圖塊編號。`m_wait(); M_LCDC=0;` 在 VBlank 中關閉 LCD，以便進行不等待的傳輸。最後的 LCDC 值選擇顯示地圖。", "El ejemplo usa `gb_common.h`: `m_init()` carga la fuente ASCII incluida con los códigos de carácter como números de tile. `m_wait(); M_LCDC=0;` apaga el LCD durante VBlank para las transferencias sin espera. El valor final de LCDC elige el mapa visible.", "O exemplo usa `gb_common.h`: `m_init()` carrega a fonte ASCII incluída, usando os códigos dos caracteres como números de tile. `m_wait(); M_LCDC=0;` desliga o LCD durante VBlank para transferências sem espera. O valor final do LCDC escolhe o mapa visível.", "L’exemple utilise `gb_common.h` : `m_init()` charge la police ASCII fournie en utilisant les codes de caractères comme numéros de tuile. `m_wait(); M_LCDC=0;` arrête le LCD pendant VBlank avant les transferts sans attente. La valeur finale de LCDC choisit la carte visible.", "Das Beispiel verwendet `gb_common.h`: `m_init()` lädt die mitgelieferte ASCII-Schrift mit Zeichencodes als Tile-Nummern. `m_wait(); M_LCDC=0;` schaltet das LCD während VBlank für Zugriffe ohne Warteprüfung aus. Der abschließende LCDC-Wert wählt die sichtbare Karte."],
  "tile_source_row_limit": ["For a horizontal copy keep `x+len <= 32`; for a vertical copy keep `y+len <= 32`. The routine does not wrap coordinates at the map edge.", "横方向なら `x+len <= 32`、縦方向なら `y+len <= 32` に収めてください。マップの端で座標を折り返す処理はありません。", "가로 복사는 `x+len <= 32`, 세로 복사는 `y+len <= 32`를 지키세요. 맵 가장자리에서 좌표가 자동으로 순환하지 않습니다.", "横向复制应满足 `x+len <= 32`，纵向复制应满足 `y+len <= 32`。不会在地图边缘自动回绕坐标。", "橫向複製應滿足 `x+len <= 32`，縱向複製應滿足 `y+len <= 32`。不會在地圖邊緣自動回繞座標。", "Para copiar horizontalmente, mantén `x+len <= 32`; verticalmente, `y+len <= 32`. No hay vuelta automática al llegar al borde del mapa.", "Na cópia horizontal, mantenha `x+len <= 32`; na vertical, `y+len <= 32`. As coordenadas não retornam automaticamente ao início ao atingir a borda do mapa.", "Pour une copie horizontale, respectez `x+len <= 32` ; pour une copie verticale, `y+len <= 32`. Les coordonnées ne rebouclent pas au bord de la carte.", "Waagerecht muss `x+len <= 32`, senkrecht `y+len <= 32` gelten. An Kartenrändern findet kein automatischer Koordinatenumbruch statt."]
}

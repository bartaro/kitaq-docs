{
  "vm_named_alias": [
    "`{name}` is a compiler alias for `{target}`. It uses the same byte-count argument and the same immediate CPU transfer, including its LCD-access rules.",
    "`{name}`は、コンパイラで`{target}`と同じ処理に展開される別名です。長さの単位は同じくバイトで、即時のCPU転送とLCDへのアクセス条件も同じです。",
    "`{name}`은 컴파일러가 `{target}`과 같은 처리로 바꾸는 별칭입니다. 길이 단위는 동일하게 바이트이며 즉시 CPU 전송과 LCD 접근 조건도 같습니다.",
    "`{name}`是编译器中`{target}`的别名。长度同样以字节计，使用相同的即时CPU传输及LCD访问规则。",
    "`{name}`是編譯器中`{target}`的別名。長度同樣以位元組計，使用相同的即時CPU傳輸及LCD存取規則。",
    "`{name}` es un alias del compilador para `{target}`. Usa el mismo recuento en bytes y la misma transferencia inmediata por CPU, incluidas las reglas de acceso al LCD.",
    "`{name}` é um alias do compilador para `{target}`. Usa a mesma contagem em bytes e a mesma transferência imediata pela CPU, incluindo as regras de acesso ao LCD.",
    "`{name}` est un alias du compilateur pour `{target}`. Le compte reste en octets, avec le même transfert immédiat par CPU et les mêmes règles d’accès au LCD.",
    "`{name}` ist ein Compileralias für `{target}`. Es verwendet dieselbe Byteanzahl und dieselbe sofortige CPU-Übertragung einschließlich der LCD-Zugriffsregeln."
  ],
  "vm_no_dma": [
    "The name does not select a DMA engine or schedule work for a later HBlank. The caller waits for completion. With the LCD active, the same CPU polling accepts either mode 0 (HBlank) or mode 1 (VBlank); CGB HDMA is not configured.",
    "この名前でDMAエンジンを選んだり、後のHBlankに処理を予約したりはしません。呼び出し元は完了まで待ちます。LCD動作中は共通のCPUポーリングでモード0（HBlank）または1（VBlank）を待ち、CGBのHDMAは設定しません。",
    "이 이름으로 DMA 엔진을 선택하거나 나중의 HBlank에 작업을 예약하지 않습니다. 호출한 쪽은 완료까지 기다립니다. LCD가 켜져 있으면 같은 CPU 폴링으로 모드 0(HBlank) 또는 1(VBlank)을 기다리며 CGB HDMA는 설정하지 않습니다.",
    "此名称不会选择DMA引擎，也不会把工作安排到之后的HBlank。调用者会等待完成。LCD活动时，相同的CPU轮询接受模式0（HBlank）或模式1（VBlank），不配置CGB HDMA。",
    "此名稱不會選擇DMA引擎，也不會把工作安排到之後的HBlank。呼叫端會等待完成。LCD運作時，相同的CPU輪詢接受模式0（HBlank）或模式1（VBlank），不設定CGB HDMA。",
    "El nombre no selecciona un motor DMA ni programa trabajo para otro HBlank. La llamada espera hasta terminar. Con el LCD activo, el mismo sondeo por CPU acepta el modo 0 (HBlank) o 1 (VBlank); no se configura HDMA de CGB.",
    "O nome não seleciona um mecanismo DMA nem agenda trabalho para outro HBlank. A chamada espera até terminar. Com o LCD ativo, a mesma consulta pela CPU aceita o modo 0 (HBlank) ou 1 (VBlank); não configura HDMA do CGB.",
    "Le nom ne sélectionne pas de moteur DMA et ne programme rien pour un HBlank ultérieur. L’appel attend la fin. LCD actif, le même polling CPU accepte le mode 0 (HBlank) ou 1 (VBlank) ; le HDMA du CGB n’est pas configuré.",
    "Der Name wählt keine DMA-Einheit aus und plant keine Arbeit für einen späteren HBlank. Der Aufrufer wartet bis zum Abschluss. Bei aktivem LCD akzeptiert dieselbe CPU-Abfrage Modus 0 (HBlank) oder 1 (VBlank); CGB-HDMA wird nicht eingerichtet."
  ],
  "vm_map_alias": [
    "Supply an explicit starting address and byte count. This alias neither chooses the background map nor fills a whole map automatically. For one complete 32 × 32 tile-number map, use its base address and 1024 bytes with VRAM bank 0 selected.",
    "開始アドレスとバイト数を明示してください。この別名関数は、背景マップを選択したり、マップ全体を自動で埋めたりはしません。32×32のタイル番号マップ全体を埋める場合は、VRAMバンク0を選択し、マップの基底アドレスと1024バイトを指定します。",
    "시작 주소와 바이트 수를 명시하세요. 이 별칭은 배경 맵을 선택하거나 전체 맵을 자동으로 채우지 않습니다. 32 × 32 타일 번호 맵 전체를 채우려면 VRAM 뱅크 0을 선택하고 맵 시작 주소와 1024바이트를 지정하세요.",
    "必须明确指定起始地址和字节数。此别名既不选择背景映射表，也不自动填充整张表。填充完整32×32图块编号表时，应选择VRAM存储体0，指定表基址和1024字节。",
    "必須明確指定起始位址和位元組數。此別名既不選擇背景對映表，也不自動填滿整張表。填滿完整32×32圖塊編號表時，應選擇VRAM記憶體庫0，指定表基底位址和1024位元組。",
    "Indica la dirección inicial y el recuento de bytes. Este alias no elige el mapa de fondo ni lo rellena entero automáticamente. Para un mapa completo de 32 × 32 índices, selecciona el banco VRAM 0 y usa la dirección base y 1024 bytes.",
    "Informe o endereço inicial e a contagem de bytes. Este alias não escolhe o mapa de fundo nem o preenche inteiro automaticamente. Para um mapa completo de 32 × 32 índices, selecione o banco VRAM 0 e use o endereço base e 1024 bytes.",
    "Indiquez l’adresse initiale et le nombre d’octets. Cet alias ne choisit pas la carte de fond et ne la remplit pas automatiquement en entier. Pour une carte complète de 32 × 32 indices, sélectionnez la banque VRAM 0, puis fournissez la base et 1024 octets.",
    "Geben Sie Startadresse und Byteanzahl ausdrücklich an. Dieser Alias wählt weder die Hintergrundkarte aus noch füllt er automatisch die ganze Karte. Für eine vollständige Karte mit 32 × 32 Kachelnummern wählen Sie VRAM-Bank 0 und geben die Basisadresse sowie 1024 Bytes an."
  ]
}

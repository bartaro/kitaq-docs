{
  "fo_index": [
    "Sprite entry number, 0–63, not a byte offset. The implementation multiplies it by four using an 8-bit cursor; out-of-range values wrap modulo 64 and can overwrite another entry. No allocation check is performed.",
    "スプライトの項目番号0〜63です。バイト単位のオフセットではありません。内部では8ビットのカーソルで4倍するため、範囲外の値は64を周期に折り返し、別の項目を上書きします。確保済みかどうかの検査は行いません。",
    "스프라이트 항목 번호 0–63입니다. 바이트 오프셋이 아닙니다. 내부에서 8비트 커서로 4를 곱하므로 범위를 벗어나면 64를 주기로 순환해 다른 항목을 덮어쓸 수 있습니다. 할당 여부는 검사하지 않습니다.",
    "精灵条目编号，范围0–63，不是字节偏移。实现使用8位游标将其乘以4；越界值会按64取模，可能覆盖其他条目。不会检查条目是否已分配。",
    "精靈項目編號，範圍0–63，不是位元組偏移。實作使用8位元游標將其乘以4；超出範圍的值會以64為週期循環，可能覆寫其他項目。不會檢查項目是否已配置。",
    "Índice de sprite entre 0 y 63, no desplazamiento en bytes. La implementación lo multiplica por cuatro con un cursor de 8 bits; los valores fuera de rango se reducen módulo 64 y pueden sobrescribir otra entrada. No se comprueba si está reservada.",
    "Índice do sprite, de 0 a 63, não um deslocamento em bytes. A implementação o multiplica por quatro usando um cursor de 8 bits; valores fora do intervalo retornam ao início a cada 64 entradas e podem sobrescrever outra entrada. Não há verificação de alocação.",
    "Indice du sprite, de 0 à 63, et non décalage en octets. Le calcul multiplie cet indice par quatre dans un curseur de 8 bits ; les valeurs hors plage sont ramenées modulo 64 et peuvent écraser une autre entrée. L’allocation n’est pas vérifiée.",
    "Sprite-Index von 0 bis 63, kein Byte-Offset. Intern wird er mit vier multipliziert und als 8-Bit-Position gespeichert. Werte außerhalb des Bereichs werden modulo 64 umgebrochen und können einen anderen Eintrag überschreiben. Eine Belegungsprüfung findet nicht statt."
  ],
  "fo_shadow": [
    "These operations edit CPU RAM at `0x0200–0x02FF`; they do not immediately change hardware OAM. Transfer the completed page with `__oam_dma`. They do not maintain the sprite library’s allocation flags, coordinate arrays or active count. Keep interrupt handlers from editing or transferring a partially prepared page.",
    "これらの操作はCPU RAMの`0x0200–0x02FF`を書き換えます。ハードウェアOAMへ即座に反映されるわけではありません。ページを完成させてから`__oam_dma`で転送してください。スプライトライブラリの確保フラグ・座標配列・使用数は更新しません。準備途中のページを割り込み処理で変更・転送しないようにします。",
    "이 작업은 CPU RAM의 `0x0200–0x02FF`를 수정하며 하드웨어 OAM에는 즉시 반영되지 않습니다. 페이지를 완성한 뒤 `__oam_dma`로 전송하세요. 스프라이트 라이브러리의 할당 플래그, 좌표 배열, 사용 개수는 갱신하지 않습니다. 준비 중인 페이지를 인터럽트 처리에서 수정하거나 전송하지 않도록 하세요.",
    "这些操作修改CPU RAM中的`0x0200–0x02FF`，不会立即改变硬件OAM。请准备好完整页面后再调用`__oam_dma`。它们不维护精灵库的分配标记、坐标数组或使用计数。应防止中断处理程序修改或传送尚未准备完毕的页面。",
    "這些操作會修改CPU RAM中的`0x0200–0x02FF`，不會立即改變硬體OAM。請準備好完整頁面後再呼叫`__oam_dma`。它們不會維護精靈函式庫的配置旗標、座標陣列或使用數量。應避免中斷處理常式修改或傳送尚未準備完成的頁面。",
    "Estas operaciones modifican la RAM de CPU `0x0200–0x02FF`; no actualizan inmediatamente la OAM del hardware. Transfiera la página completa con `__oam_dma`. No mantienen las marcas de reserva, las matrices de coordenadas ni el contador de la biblioteca de sprites. Evite que una interrupción modifique o transfiera una página a medio preparar.",
    "Essas operações alteram a RAM da CPU em `0x0200–0x02FF`; a OAM do hardware não é atualizada imediatamente. Transfira a página pronta com `__oam_dma`. Elas não mantêm os indicadores de alocação, os arrays de coordenadas ou a contagem da biblioteca de sprites. Impeça que uma interrupção modifique ou transfira uma página ainda em preparação.",
    "Ces opérations modifient la RAM CPU `0x0200–0x02FF`, sans mettre immédiatement à jour l’OAM matérielle. Transférez la page complète avec `__oam_dma`. Elles ne tiennent à jour ni les indicateurs d’allocation, ni les tableaux de coordonnées, ni le compteur de la bibliothèque de sprites. Évitez qu’une interruption modifie ou transfère une page encore en préparation.",
    "Diese Operationen ändern den CPU-RAM bei `0x0200–0x02FF`, nicht sofort das Hardware-OAM. Übertragen Sie die fertige Seite mit `__oam_dma`. Belegungsflags, Koordinatenfelder und Zähler der Sprite-Bibliothek werden nicht mitgeführt. Interrupt-Routinen dürfen eine noch unvollständige Seite weder ändern noch übertragen."
  ],
  "fo_attr_arg": [
    "Complete attribute byte: bits 0–1 select sprite palette 0–3; bit 5 places opaque sprite pixels behind opaque background pixels; bits 6 and 7 flip horizontally and vertically. Keep bits 2–4 zero. Combine flags with bitwise OR; the supplied byte replaces all previous attributes.",
    "属性バイト全体です。ビット0〜1でスプライトパレット0〜3、ビット5で不透明な背景の後ろに表示する指定、ビット6で左右反転、ビット7で上下反転を設定します。ビット2〜4は0にしてください。フラグはビットORで組み合わせ、渡したバイトで属性全体を置き換えます。",
    "속성 바이트 전체입니다. 비트 0–1은 스프라이트 팔레트 0–3, 비트 5는 불투명 배경 뒤에 표시, 비트 6과 7은 좌우 및 상하 반전을 지정합니다. 비트 2–4는 0으로 두세요. 플래그를 비트 OR로 조합하며 전달한 바이트가 기존 속성 전체를 대체합니다.",
    "完整属性字节：位0–1选择精灵调色板0–3；位5使不透明精灵像素位于不透明背景像素之后；位6和7分别控制水平与垂直翻转。位2–4应为0。可用按位OR组合标志；传入字节会替换全部原有属性。",
    "完整屬性位元組：位元0–1選擇精靈調色盤0–3；位元5使不透明精靈像素位於不透明背景像素之後；位元6與7分別控制水平及垂直翻轉。位元2–4應為0。可用位元OR組合旗標；傳入的位元組會取代全部原有屬性。",
    "Byte completo de atributos: bits 0–1 seleccionan la paleta 0–3; bit 5 coloca los píxeles opacos del sprite detrás de los del fondo; bits 6 y 7 invierten horizontal y verticalmente. Mantenga los bits 2–4 a cero. Combine banderas con OR a nivel de bits; el byte sustituye todos los atributos anteriores.",
    "Byte completo de atributos: os bits 0–1 selecionam a paleta 0–3; o bit 5 coloca pixels opacos do sprite atrás dos pixels opacos do fundo; os bits 6 e 7 espelham horizontal e verticalmente. Mantenha os bits 2–4 em zero. Combine as opções com OR bit a bit; o byte substitui todos os atributos anteriores.",
    "Octet d’attributs complet : les bits 0–1 choisissent la palette 0–3 ; le bit 5 place les pixels opaques du sprite derrière ceux du fond ; les bits 6 et 7 retournent le dessin horizontalement et verticalement. Laissez les bits 2–4 à zéro. Combinez les indicateurs par OU bit à bit ; l’octet remplace tous les attributs précédents.",
    "Vollständiges Attributbyte: Bits 0–1 wählen Palette 0–3; Bit 5 legt undurchsichtige Sprite-Pixel hinter undurchsichtige Hintergrundpixel; Bits 6 und 7 spiegeln horizontal beziehungsweise vertikal. Bits 2–4 müssen null bleiben. Kombinieren Sie Flags mit bitweisem ODER; das Byte ersetzt sämtliche bisherigen Attribute."
  ],
  "fo_page_arg": [
    "High byte of the source CPU address: pass `4` for `0x0400–0x04FF`, not `0x0400`. Supply a complete readable 256-byte page and keep any required bank mapped. Avoid pages containing I/O registers, whose reads can have side effects. No source validation is performed.",
    "転送元CPUアドレスの上位バイトです。`0x0400–0x04FF`なら`0x0400`ではなく`4`を渡します。256バイトすべてが読み出せるページを用意し、必要なバンクを選択しておいてください。読み出しに副作用があるI/Oレジスタを含むページは避けます。転送元の検査は行いません。",
    "원본 CPU 주소의 상위 바이트입니다. `0x0400–0x04FF`를 전송하려면 `0x0400`이 아니라 `4`를 전달하세요. 256바이트 전체를 읽을 수 있는 페이지를 준비하고 필요한 뱅크를 선택해 두어야 합니다. 읽기에 부수 효과가 있는 I/O 레지스터 페이지는 피하세요. 원본 유효성은 검사하지 않습니다.",
    "源CPU地址的高字节：传送`0x0400–0x04FF`时应传入`4`，而不是`0x0400`。请提供完整的256字节可读页面，并保持所需存储体已映射。避免使用含I/O寄存器的页面，因为读取可能产生副作用。函数不检查源地址。",
    "來源CPU位址的高位元組：傳送`0x0400–0x04FF`時應傳入`4`，而不是`0x0400`。請提供完整的256位元組可讀頁面，並保持所需記憶體庫已映射。避免使用含I/O暫存器的頁面，因為讀取可能產生副作用。函式不會檢查來源位址。",
    "Byte alto de la dirección CPU de origen: pase `4` para `0x0400–0x04FF`, no `0x0400`. Proporcione una página completa de 256 bytes legibles y mantenga mapeado el banco necesario. Evite páginas con registros de E/S, cuyas lecturas pueden tener efectos secundarios. No se valida el origen.",
    "Byte mais significativo do endereço de origem da CPU: passe `4` para `0x0400–0x04FF`, não `0x0400`. Forneça uma página completa de 256 bytes legíveis e mantenha o banco necessário mapeado. Evite páginas com registradores de E/S, cujas leituras podem ter efeitos colaterais. A origem não é validada.",
    "Octet de poids fort de l’adresse CPU source : passez `4` pour `0x0400–0x04FF`, et non `0x0400`. Fournissez une page complète de 256 octets lisibles et gardez le banc requis accessible. Évitez les pages contenant des registres d’E/S, dont la lecture peut avoir des effets de bord. La source n’est pas validée.",
    "Oberes Byte der CPU-Quelladresse: Übergeben Sie für `0x0400–0x04FF` den Wert `4`, nicht `0x0400`. Stellen Sie eine vollständige lesbare Seite mit 256 Byte bereit und halten Sie die nötige Bank eingeblendet. Vermeiden Sie Seiten mit E/A-Registern, deren Lesezugriffe Nebenwirkungen haben können. Die Quelle wird nicht geprüft."
  ],
  "fo_stream_arg": [
    "Pointer to readable four-byte records: `dx, dy, tile, attr`. A byte `0xFF` in the next dx position ends the stream; no other bytes are needed after it. Offsets add modulo 256: `0xFE` represents −2 and dy=`0xFF` can represent −1, but dx=`0xFF` is reserved and cannot represent −1. The pointer must remain valid in the currently mapped memory.",
    "`dx, dy, tile, attr`の4バイトを1組とする読み出し可能なデータ列へのポインタです。次のdxの位置に`0xFF`があれば終了し、その後のバイトは不要です。オフセットは256を周期に加算します。`0xFE`は−2、dyの`0xFF`は−1として使えますが、dxの`0xFF`は終端専用なので−1を表せません。現在選択中のメモリでポインタが有効である必要があります。",
    "`dx, dy, tile, attr`의 4바이트 레코드로 이루어진 읽기 가능한 데이터열의 포인터입니다. 다음 dx 위치의 `0xFF`가 종료를 나타내며 그 뒤의 바이트는 필요 없습니다. 오프셋은 256을 주기로 더합니다. `0xFE`는 −2, dy의 `0xFF`는 −1로 사용할 수 있지만 dx의 `0xFF`는 종료 전용입니다. 포인터는 현재 매핑된 메모리에서 유효해야 합니다.",
    "指向可读四字节记录的指针，记录格式为`dx, dy, tile, attr`。下一个dx位置的`0xFF`表示结束，其后无需其他字节。偏移按256取模相加：`0xFE`表示−2，dy的`0xFF`可表示−1，但dx的`0xFF`保留作终止标记，不能表示−1。指针必须在当前映射的内存中有效。",
    "指向可讀四位元組記錄的指標，格式為`dx, dy, tile, attr`。下一個dx位置的`0xFF`表示結束，其後無須其他位元組。偏移以256為週期相加：`0xFE`表示−2，dy的`0xFF`可表示−1，但dx的`0xFF`保留作終止標記，不能表示−1。指標必須在目前映射的記憶體中有效。",
    "Puntero a registros legibles de cuatro bytes: `dx, dy, tile, attr`. Un `0xFF` en la siguiente posición dx termina la secuencia; no necesita bytes posteriores. Los desplazamientos se suman módulo 256: `0xFE` representa −2 y dy=`0xFF` puede representar −1, pero dx=`0xFF` está reservado y no puede representar −1. El puntero debe ser válido en la memoria mapeada.",
    "Ponteiro para registros legíveis de quatro bytes: `dx, dy, tile, attr`. Um `0xFF` na próxima posição dx encerra a sequência; não são necessários bytes depois dele. Os deslocamentos são somados módulo 256: `0xFE` representa −2 e dy=`0xFF` pode representar −1, mas dx=`0xFF` é reservado e não pode representar −1. O ponteiro deve ser válido na memória mapeada.",
    "Pointeur vers des enregistrements lisibles de quatre octets : `dx, dy, tile, attr`. Un `0xFF` à la position dx suivante termine le flux ; aucun octet supplémentaire n’est nécessaire. Les décalages s’ajoutent modulo 256 : `0xFE` représente −2 et dy=`0xFF` peut représenter −1, mais dx=`0xFF` est réservé au terminateur. Le pointeur doit être valide dans la mémoire actuellement accessible.",
    "Zeiger auf lesbare Datensätze aus vier Byte: `dx, dy, tile, attr`. Ein `0xFF` an der nächsten dx-Position beendet die Folge; weitere Bytes sind nicht nötig. Offsets werden modulo 256 addiert: `0xFE` steht für −2 und dy=`0xFF` darf −1 darstellen. dx=`0xFF` ist hingegen die Endmarkierung und kann nicht für −1 verwendet werden. Der Zeiger muss im eingeblendeten Speicher gültig sein."
  ],
  "fo_meta_limit": [
    "There is no part count, clipping or capacity check. Include a terminator and use at most `64−oam_index` parts. Both coordinates wrap modulo 256; after slot 63 the write cursor wraps to slot 0. An empty stream changes no entries. This stream format differs from the counted `MetaSpritePart` array used by the sprite library’s `metasprite_draw`.",
    "部品数・画面外クリッピング・容量の検査はありません。必ず終端を付け、部品数を`64−oam_index`以下にしてください。座標は256を周期に折り返し、63番の次は0番へ書き込みます。空のデータ列は何も変更しません。この形式は、スプライトライブラリの`metasprite_draw`で使う個数指定の`MetaSpritePart`配列とは異なります。",
    "부품 수, 화면 잘라내기, 용량을 검사하지 않습니다. 종료 표시를 반드시 넣고 부품 수를 `64−oam_index` 이하로 제한하세요. 좌표는 256을 주기로 순환하며 63번 다음에는 0번에 기록합니다. 빈 데이터열은 아무 항목도 변경하지 않습니다. 이 형식은 스프라이트 라이브러리의 `metasprite_draw`가 사용하는 개수 지정 `MetaSpritePart` 배열과 다릅니다.",
    "不检查部件数、屏幕裁剪或容量。必须添加终止标记，并将部件数限制为不超过`64−oam_index`。坐标按256取模；写完槽位63后会回到槽位0。空数据流不修改任何条目。此格式不同于精灵库`metasprite_draw`使用的、另行指定数量的`MetaSpritePart`数组。",
    "不會檢查部件數、畫面裁切或容量。必須加入終止標記，並將部件數限制在`64−oam_index`以內。座標以256為週期循環；寫完項目63後會回到項目0。空資料串流不會修改任何項目。此格式不同於精靈函式庫`metasprite_draw`使用的、另外指定數量的`MetaSpritePart`陣列。",
    "No comprueba el número de piezas, los límites de pantalla ni la capacidad. Incluya un terminador y como máximo `64−oam_index` piezas. Las coordenadas se reducen módulo 256; tras la entrada 63 se escribe en la 0. Una secuencia vacía no cambia entradas. Este formato difiere del array `MetaSpritePart` con recuento explícito que usa `metasprite_draw` en la biblioteca de sprites.",
    "Não há verificação da quantidade de partes, recorte de tela ou capacidade. Inclua o terminador e use no máximo `64−oam_index` partes. As coordenadas são reduzidas módulo 256; após a entrada 63, a escrita volta à entrada 0. Uma sequência vazia não altera entradas. Esse formato difere do array `MetaSpritePart` com quantidade explícita usado por `metasprite_draw` na biblioteca de sprites.",
    "Le nombre de parties, le découpage à l’écran et la capacité ne sont pas vérifiés. Ajoutez un terminateur et limitez-vous à `64−oam_index` parties. Les coordonnées sont ramenées modulo 256 ; après l’entrée 63, l’écriture reprend à l’entrée 0. Un flux vide ne modifie rien. Ce format diffère du tableau `MetaSpritePart` à nombre explicite utilisé par `metasprite_draw` dans la bibliothèque de sprites.",
    "Anzahl der Teile, Bildausschnitt und Kapazität werden nicht geprüft. Fügen Sie eine Endmarkierung ein und verwenden Sie höchstens `64−oam_index` Teile. Koordinaten laufen modulo 256 über; nach Eintrag 63 wird bei Eintrag 0 weitergeschrieben. Eine leere Folge ändert nichts. Das Format unterscheidet sich vom `MetaSpritePart`-Array mit expliziter Anzahl, das `metasprite_draw` in der Sprite-Bibliothek verwendet."
  ],
  "fo_meta_return": [
    "Next OAM entry number, `(oam_index + parts_written) modulo 64`. This is a cursor, not the number of parts drawn. For an empty stream it is `oam_index modulo 64`.",
    "次のOAM項目番号で、`(oam_index + 書き込んだ部品数) modulo 64`です。描いた部品数ではなく、次の書き込み位置を返します。空のデータ列では`oam_index modulo 64`です。",
    "다음 OAM 항목 번호인 `(oam_index + 기록한 부품 수) modulo 64`를 반환합니다. 그린 부품 수가 아니라 다음 기록 위치입니다. 빈 데이터열에서는 `oam_index modulo 64`입니다.",
    "下一个OAM条目编号，即`(oam_index + 已写部件数) modulo 64`。返回的是游标，而不是绘制的部件数。空数据流返回`oam_index modulo 64`。",
    "下一個OAM項目編號，即`(oam_index + 已寫入部件數) modulo 64`。傳回的是游標，而不是繪製的部件數。空資料串流會傳回`oam_index modulo 64`。",
    "Siguiente índice OAM: `(oam_index + piezas_escritas) modulo 64`. Es un cursor, no la cantidad de piezas dibujadas. Una secuencia vacía devuelve `oam_index modulo 64`.",
    "Próximo índice OAM: `(oam_index + partes_gravadas) modulo 64`. É um cursor, não a quantidade de partes desenhadas. Uma sequência vazia retorna `oam_index modulo 64`.",
    "Indice OAM suivant : `(oam_index + parties_écrites) modulo 64`. Il s’agit d’un curseur, pas du nombre de parties dessinées. Un flux vide renvoie `oam_index modulo 64`.",
    "Nächster OAM-Index: `(oam_index + geschriebene_Teile) modulo 64`. Das ist eine Schreibposition, nicht die Anzahl gezeichneter Teile. Bei einer leeren Folge wird `oam_index modulo 64` zurückgegeben."
  ]
}
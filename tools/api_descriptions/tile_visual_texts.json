{
  "tile_example_blue": ["On CGB the `A` is blue: attribute value 2 selects the blue palette prepared by this example. On DMG the same character is black because CGB attributes are not applied.", "CGBでは、属性値2によりサンプルで用意した青のパレットが選ばれ、`A` が青く表示されます。DMGではCGB属性を適用しないので、同じ文字が黒く表示されます。", "CGB에서는 속성값 2가 예제에서 준비한 파란색 팔레트를 선택하므로 `A`가 파란색으로 표시됩니다. DMG에는 CGB 속성을 적용하지 않아 같은 문자가 검은색입니다.", "CGB 上的 `A` 为蓝色：属性值 2 选择示例设置的蓝色调色板。DMG 不应用 CGB 属性，因此同一字符为黑色。", "CGB 上的 `A` 為藍色：屬性值 2 選擇範例設定的藍色調色盤。DMG 不套用 CGB 屬性，因此同一字元為黑色。", "En CGB, la `A` es azul: el atributo 2 selecciona la paleta azul preparada por el ejemplo. En DMG, el carácter es negro porque no se aplican los atributos CGB.", "No CGB, o `A` é azul: o atributo 2 seleciona a paleta azul preparada pelo exemplo. No DMG, o caractere é preto porque os atributos do CGB não são aplicados.", "Sur CGB, le `A` est bleu : l’attribut 2 sélectionne la palette bleue préparée par l’exemple. Sur DMG, le même caractère est noir, car les attributs CGB ne s’appliquent pas.", "Auf CGB erscheint das `A` blau: Attributwert 2 wählt die im Beispiel vorbereitete blaue Palette. Auf DMG ist dasselbe Zeichen schwarz, da CGB-Attribute dort nicht angewendet werden."],
  "tile_example_rgb": ["On CGB, `A`, `B` and `C` are red, blue and green respectively: attribute bytes `1,2,3` select three different prepared palettes. On DMG all three are black.", "CGBでは、属性列 `1,2,3` がそれぞれ用意したパレットを選び、`A` は赤、`B` は青、`C` は緑になります。DMGでは3文字とも黒です。", "CGB에서는 속성 배열 `1,2,3`이 각각 준비된 팔레트를 선택하여 `A`는 빨간색, `B`는 파란색, `C`는 초록색이 됩니다. DMG에서는 세 문자 모두 검은색입니다.", "CGB 上，属性字节 `1,2,3` 分别选择三个预设调色板，使 `A` 为红色、`B` 为蓝色、`C` 为绿色。DMG 上三个字符均为黑色。", "CGB 上，屬性位元組 `1,2,3` 分別選擇三個預設調色盤，使 `A` 為紅色、`B` 為藍色、`C` 為綠色。DMG 上三個字元均為黑色。", "En CGB, `A`, `B` y `C` son roja, azul y verde: los atributos `1,2,3` seleccionan tres paletas distintas. En DMG, las tres letras son negras.", "No CGB, `A`, `B` e `C` são vermelho, azul e verde: os atributos `1,2,3` selecionam três paletas diferentes. No DMG, as três letras são pretas.", "Sur CGB, `A`, `B` et `C` sont respectivement rouge, bleu et vert : les attributs `1,2,3` sélectionnent trois palettes distinctes. Sur DMG, les trois lettres sont noires.", "Auf CGB erscheinen `A`, `B` und `C` rot, blau und grün: Die Attributbytes `1,2,3` wählen drei unterschiedliche Paletten. Auf DMG sind alle drei Buchstaben schwarz."],
  "tile_image_layout": ["Expected image: the following tile pattern starts at tile coordinates `(x={x}, y={y})`, or pixel coordinates `({px},{py})`. Each character occupies one 8×8 tile. Read the pattern from top to bottom and left to right; the surrounding tiles remain blank.", "期待画像：次のタイル配置が、タイル座標 `(x={x}, y={y})`、ピクセル座標 `({px},{py})` から表示されます。1文字が8×8の1タイルです。上から下、左から右に読み、周囲のタイルは空白になります。", "예상 이미지: 다음 타일 패턴이 타일 좌표 `(x={x}, y={y})`, 픽셀 좌표 `({px},{py})`에서 시작합니다. 문자 하나는 8×8 타일 하나입니다. 위에서 아래로, 왼쪽에서 오른쪽으로 읽으며 주변 타일은 비어 있습니다.", "预期图像：以下图块排列从图块坐标 `(x={x}, y={y})`、像素坐标 `({px},{py})` 开始显示。每个字符占一个 8×8 图块。按从上到下、从左到右阅读，周围图块保持空白。", "預期影像：以下圖塊排列從圖塊座標 `(x={x}, y={y})`、像素座標 `({px},{py})` 開始顯示。每個字元占一個 8×8 圖塊。由上至下、由左至右閱讀，周圍圖塊保持空白。", "Imagen esperada: el patrón siguiente empieza en las coordenadas de tile `(x={x}, y={y})`, equivalentes al píxel `({px},{py})`. Cada carácter ocupa un tile de 8×8. Lee de arriba abajo y de izquierda a derecha; los tiles circundantes quedan vacíos.", "Imagem esperada: o padrão abaixo começa nas coordenadas de tile `(x={x}, y={y})`, equivalentes ao pixel `({px},{py})`. Cada caractere ocupa um tile de 8×8. Leia de cima para baixo e da esquerda para a direita; os tiles ao redor ficam vazios.", "Image attendue : le motif ci-dessous commence aux coordonnées de tuile `(x={x}, y={y})`, soit au pixel `({px},{py})`. Chaque caractère occupe une tuile de 8×8. Lisez de haut en bas et de gauche à droite ; les tuiles voisines restent vides.", "Erwartetes Bild: Das folgende Tile-Muster beginnt bei Tile-Koordinaten `(x={x}, y={y})`, entsprechend Pixel `({px},{py})`. Jedes Zeichen belegt ein 8×8-Tile. Lesen Sie von oben nach unten und links nach rechts; die umgebenden Tiles bleiben leer."]
}

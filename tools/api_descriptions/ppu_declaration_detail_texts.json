{
  "pd_controls": [
    "Raw PPUCTRL and PPUMASK bytes. For example, ctrl=0x80 enables NMI with background pattern table zero; mask=0x0A enables the background including its leftmost eight pixels.",
    "PPUCTRLとPPUMASKへ書くバイト値です。例のctrl=0x80はNMI有効・背景パターンテーブル0、mask=0x0Aは左端8ピクセルを含む背景描画を表します。",
    "선언에서 `ctrl`과 `mask`는 부호 없는 8비트 형식입니다. 실행 동작을 정의하는 함수 본체는 없습니다. 동작하는 대안은 별도의 내장 함수로 원하는 PPUCTRL과 PPUMASK 값을 씁니다.",
    "声明中的 `ctrl` 和 `mask` 为无符号八位类型，没有函数体定义其运行时行为。可运行替代方案通过两个内置函数写入所需的 PPUCTRL 和 PPUMASK 值。",
    "宣告中的 `ctrl` 和 `mask` 為無號八位元型別，沒有函式本體定義其執行行為。可執行替代方案透過兩個內建函式寫入所需的 PPUCTRL 和 PPUMASK 值。",
    "La declaración asigna a `ctrl` y `mask` tipos de ocho bits sin signo. Ningún cuerpo define su comportamiento en ejecución. La alternativa funcional escribe los valores de PPUCTRL y PPUMASK mediante llamadas intrínsecas separadas.",
    "A declaração define `ctrl` e `mask` como tipos de oito bits sem sinal. Não existe corpo que defina seu comportamento em execução. A alternativa funcional grava os valores de PPUCTRL e PPUMASK com chamadas intrínsecas separadas.",
    "La déclaration donne à `ctrl` et `mask` des types non signés sur huit bits. Aucun corps ne définit leur comportement à l’exécution. La solution fonctionnelle écrit PPUCTRL et PPUMASK par deux appels intrinsèques distincts.",
    "Die Deklaration gibt `ctrl` und `mask` vorzeichenlose 8-Bit-Typen. Ein Funktionskörper, der ihr Laufzeitverhalten festlegt, fehlt. Die lauffähige Alternative schreibt die gewünschten PPUCTRL- und PPUMASK-Werte durch getrennte Intrinsic-Aufrufe."
  ],
  "pd_pal_arg": [
    "Pointer to 32 readable color-code bytes: background first, then sprites. Keep the source bank mapped until return. Palette mirroring applies, including the universal background color aliases.",
    "読み出せる色コード32バイトへのポインタです。先に背景、次にスプライトを並べます。戻るまで素材のバンクを保持してください。共通背景色の別名アドレスなど、PPUのパレットミラーリングが適用されます。",
    "`pal32`는 부호 없는 바이트 포인터로 선언되어 있습니다. 이름에 32가 있어도 32바이트 전송 구현이 제공되는 것은 아닙니다. 동작하는 대안에서는 각 팔레트 내장 함수에 읽을 수 있는 16바이트를 제공하고 반환할 때까지 원본 뱅크를 유지하세요.",
    "`pal32` 声明为无符号字节指针，名称并不代表已经实现了 32 字节传输。使用可运行替代方案时，为每个调色板内置函数提供 16 个可读字节，并在返回前保持源存储体映射。",
    "`pal32` 宣告為無號位元組指標，名稱並不代表已經實作了 32 位元組傳輸。使用可執行替代方案時，為每個調色盤內建函式提供 16 個可讀位元組，並在返回前保持來源記憶體庫映射。",
    "`pal32` se declara como puntero a bytes sin signo. El nombre no aporta una transferencia implementada de 32 bytes. Para la alternativa funcional, suministre 16 bytes legibles a cada intrínseca de paleta y mantenga el banco de origen mapeado hasta el retorno.",
    "`pal32` é declarado como ponteiro para bytes sem sinal. O nome não fornece uma transferência implementada de 32 bytes. Na alternativa funcional, forneça 16 bytes legíveis a cada intrínseca de paleta e mantenha o banco de origem mapeado até o retorno.",
    "`pal32` est déclaré comme pointeur vers des octets non signés. Ce nom ne fournit pas de transfert implémenté de 32 octets. Pour la solution fonctionnelle, fournissez 16 octets lisibles à chaque intrinsèque de palette et maintenez la banque source accessible jusqu’au retour.",
    "`pal32` ist als Zeiger auf vorzeichenlose Bytes deklariert. Der Name stellt keine implementierte 32-Byte-Übertragung bereit. Übergeben Sie für die lauffähige Alternative jedem Paletten-Intrinsic 16 lesbare Bytes und halten Sie die Quellbank bis zur Rückkehr eingeblendet."
  ],
  "pd_nt_args": [
    "nt_base must be 0x2000, 0x2400, 0x2800 or 0x2C00. tile is the tile number for all 960 entries; attr is the packed four-quadrant palette byte for all 64 attribute entries. Cartridge mirroring determines which logical tables share memory.",
    "nt_baseは0x2000・0x2400・0x2800・0x2C00のいずれかです。tileは960マス共通のタイル番号、attrは属性64バイト共通の4区画のパレット選択値です。論理テーブル同士のメモリ共有はカートリッジのミラーリング設定で決まります。",
    "프로토타입에서 `nt_base`는 부호 없는 16비트, `tile`과 `attr`는 부호 없는 8비트입니다. 이 매개변수의 주소 선택, 채우기, 검증을 구현하는 본체는 없습니다. 대안은 논리 네임테이블 0을 명시적으로 선택하고 타일과 압축된 속성 값을 따로 지정합니다.",
    "原型将 `nt_base` 声明为无符号 16 位，`tile` 和 `attr` 声明为无符号八位。没有函数体实现这些参数的地址选择、填充或校验。替代方案明确选择逻辑名称表 0，并分别提供图块和打包属性值。",
    "原型將 `nt_base` 宣告為無號 16 位元，`tile` 和 `attr` 宣告為無號八位元。沒有函式本體實作這些參數的位址選擇、填滿或驗證。替代方案明確選擇邏輯名稱表 0，並分別提供圖塊和打包屬性值。",
    "El prototipo declara `nt_base` como entero sin signo de 16 bits y `tile`/`attr` como enteros sin signo de ocho bits. No hay cuerpo que implemente selección de dirección, relleno o validación. La alternativa elige explícitamente la tabla lógica 0 y proporciona por separado los valores de tile y atributo empaquetado.",
    "O protótipo declara `nt_base` como inteiro sem sinal de 16 bits e `tile`/`attr` como inteiros sem sinal de oito bits. Não há corpo que implemente seleção de endereço, preenchimento ou validação. A alternativa escolhe explicitamente a tabela lógica 0 e fornece separadamente os valores de tile e atributo compactado.",
    "Le prototype déclare `nt_base` sur 16 bits non signés et `tile`/`attr` sur huit bits non signés. Aucun corps n’implémente la sélection d’adresse, le remplissage ou la validation. La solution de remplacement choisit explicitement la table logique 0 et fournit séparément les valeurs de tuile et d’attribut compacté.",
    "Der Prototyp deklariert `nt_base` als vorzeichenlosen 16-Bit-Wert und `tile`/`attr` als vorzeichenlose 8-Bit-Werte. Es gibt keinen Funktionskörper für Adresswahl, Füllen oder Prüfung dieser Parameter. Die Alternative wählt ausdrücklich die logische Nametable 0 und übergibt Kachel- und gepackte Attributwerte getrennt."
  ],
  "pd_return": [
    "No return value. Writes complete synchronously.",
    "戻り値はありません。書き込みは呼び出し内で完了します。",
    "선언된 반환 형식은 `void`입니다. 링크할 구현이 없으므로 이 이름에 대해 설명할 실행 가능한 반환 동작은 없습니다.",
    "声明的返回类型为 `void`。没有可链接的实现，因此此名称没有可执行的返回行为。",
    "宣告的回傳型別為 `void`。沒有可連結的實作，因此此名稱沒有可執行的返回行為。",
    "Tipo de retorno declarado: `void`. No hay implementación enlazada ni comportamiento de retorno ejecutable para este nombre.",
    "Tipo de retorno declarado: `void`. Não há implementação vinculada nem comportamento de retorno executável para esse nome.",
    "Type de retour déclaré : `void`. Aucune implémentation n’est liée ; il n’existe donc pas de comportement de retour exécutable pour ce nom.",
    "Deklarierter Rückgabetyp: `void`. Es ist keine Implementierung eingebunden; für diesen Namen gibt es daher kein ausführbares Rückkehrverhalten."
  ],
  "pd_scope": [
    "Compile ppu.c and include ppu.h. Bulk transfers require rendering off; these routines do not wait for VBlank or queue writes. Prevent concurrent PPU access and restore scroll before displaying. Set control/mask through library or intrinsic setters so their software shadows remain correct; direct register writes bypass those shadows.",
    "ppu.cをコンパイルし、ppu.hを読み込みます。一括転送は描画を止めて実行してください。VBlank待機やキュー登録はしません。同時のPPUアクセスを避け、表示前にスクロールを設定します。制御・マスクはライブラリまたは組み込み関数で設定してください。レジスターへの直接書き込みはソフトウェア側の保持値へ反映されません。",
    "샘플은 구현된 대안을 보여주며 해당 선언의 호출 성공 예제가 아닙니다. 검증에는 각 미구현 이름의 미해결 심벌 빌드와 대안의 별도 에뮬레이터 검사가 포함됩니다. 선언된 이름을 호출하려면 일치하는 구현을 직접 제공해야 하며 다른 헤더를 포함하는 것만으로 본체가 생기지는 않습니다.",
    "示例展示的是已经实现的替代方案，不是成功调用这些声明。验证包含每个缺失名称触发未解析符号的预期构建结果，以及对替代方案的独立模拟器检查。若要调用声明中的名称，请自行提供匹配实现；仅包含另一个头文件不会添加函数体。",
    "範例展示的是已經實作的替代方案，不是成功呼叫這些宣告。驗證包含每個缺失名稱觸發未解析符號的預期建置結果，以及對替代方案的獨立模擬器檢查。若要呼叫宣告中的名稱，請自行提供相符實作；僅包含另一個標頭檔不會加入函式本體。",
    "El ejemplo muestra alternativas implementadas, no una llamada exitosa a la declaración. La verificación incluye el fallo esperado por símbolo sin resolver de cada nombre ausente y pruebas independientes de las alternativas en el emulador. Para llamar al nombre declarado, proporcione una implementación compatible; incluir otro encabezado no crea el cuerpo.",
    "O exemplo mostra alternativas implementadas, não uma chamada bem-sucedida à declaração. A verificação inclui a falha esperada por símbolo não resolvido de cada nome ausente e testes separados das alternativas no emulador. Para chamar o nome declarado, forneça uma implementação compatível; incluir outro cabeçalho não cria o corpo.",
    "L’exemple présente des solutions implémentées, pas un appel réussi à la déclaration. La vérification comprend l’échec attendu pour symbole non résolu de chaque nom absent et des contrôles distincts des solutions dans l’émulateur. Pour appeler le nom déclaré, fournissez une implémentation compatible ; inclure un autre en-tête ne crée pas le corps.",
    "Das Beispiel zeigt implementierte Alternativen und keinen erfolgreichen Aufruf der Deklaration. Die Prüfung umfasst den erwarteten Buildfehler wegen eines unaufgelösten Symbols für jeden fehlenden Namen sowie separate Emulatorprüfungen der Alternativen. Stellen Sie zum Aufruf des deklarierten Namens eine passende eigene Implementierung bereit; ein weiteres Header-Include liefert keinen Funktionskörper."
  ]
}
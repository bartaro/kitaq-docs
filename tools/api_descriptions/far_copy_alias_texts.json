{
  "fca_alias": {
    "ja": "`__farmemcpy`はFCの`__far_memcpy`を別の綴りで呼ぶ組み込み関数です。引数の順序、バンクの復元、コピーする内容は同じで、どちらを選んでも転送方式や速度は変わりません。サンプルではこの綴りを使って、ROMの絵柄をRAMへ読み出します。",
    "en": "`__farmemcpy` is an alternative spelling of the FC intrinsic `__far_memcpy`. It takes the same arguments and uses the same copy and bank-restoration routine; choosing this spelling does not select a different transfer method or speed. The example uses it to read tile patterns from ROM into RAM."
  },
  "fca_declaration": {
    "ja": "この綴りはコンパイラに組み込み登録されています。`intrinsics.h`に別の宣言がなくても呼び出せます。引数は順にRAMポインター、8ビットのバンク番号、16ビットのCPUアドレス、16ビットのバイト数です。`src`はROMファイル先頭からのオフセットではありません。",
    "en": "The compiler registers this spelling directly, so it can be called without a separate declaration in `intrinsics.h`. Arguments are a RAM pointer, an 8-bit bank number, a 16-bit CPU address and a 16-bit byte count, in that order. `src` is not an offset from the start of the ROM file."
  },
  "fca_zero": {
    "ja": "`len`が0ならコピー先のバイトは変更されませんが、バンク選択と復元の処理は実行されます。戻り値はありません。",
    "en": "When `len` is zero, no destination bytes are changed, but bank selection and restoration still take place. The function returns no value."
  },
  "fca_pairs": {
    "ja": "FC画面の(16,64)にある青い三角形と正方形の枠は`__farmemcpy`でコピーした絵柄です。その下の(16,96)にある緑の組は`asset_load_raw`の結果です。黒地の各組は16×8ピクセルで、コピー後にCHR RAMへ転送して表示します。",
    "en": "The blue triangle and square outline at pixel (16,64) show the bytes copied by `__farmemcpy`. The green pair below at (16,96) comes from `asset_load_raw`. Each pair occupies 16×8 pixels on black; the copied bytes are uploaded to CHR RAM for display."
  }
}
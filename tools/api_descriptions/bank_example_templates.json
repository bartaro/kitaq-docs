{
  "read": [
    "This excerpt uses `{api}` to read `{source}` in bank 3 and checks `{value}`. In the full program, the near read from `home2` also confirms that the bank-2 mapping is restored.",
    "この断片では`{api}`でバンク3の`{source}`を読み、`{value}`を確認します。完成プログラムでは、`home2`の近ポインター読み出しでもバンク2への復帰を確認します。",
    "이 코드 조각은 `{api}`로 뱅크 3의 `{source}`를 읽고 `{value}`를 확인합니다. 완성 프로그램은 `home2`의 근거리 읽기로 뱅크 2 매핑이 복원되었는지도 확인합니다.",
    "此代码片段使用 `{api}` 读取存储体 3 中的 `{source}` 并检查 `{value}`。完整程序还通过近指针读取 `home2`，确认已恢复存储体 2 的映射。",
    "此程式片段使用 `{api}` 讀取區塊 3 中的 `{source}` 並檢查 `{value}`。完整程式還透過近指標讀取 `home2`，確認已還原區塊 2 的映射。",
    "Este fragmento usa `{api}` para leer `{source}` del banco 3 y comprueba `{value}`. El programa completo también lee `home2` con un puntero cercano para confirmar que se ha restaurado el banco 2.",
    "Este trecho usa `{api}` para ler `{source}` no banco 3 e verifica `{value}`. O programa completo também lê `home2` com um ponteiro próximo para confirmar a restauração do banco 2.",
    "Cet extrait utilise `{api}` pour lire `{source}` dans la banque 3 et vérifie `{value}`. Le programme complet lit aussi `home2` avec un pointeur proche pour confirmer la restauration de la banque 2.",
    "Dieser Ausschnitt liest mit `{api}` den Ausdruck `{source}` in Bank 3 und prüft `{value}`. Das vollständige Programm bestätigt durch einen Nahzugriff auf `home2` außerdem die Wiederherstellung von Bank 2."
  ],
  "copy": [
    "This excerpt copies {count} bytes through `{api}` and checks the buffer against `{bytes}`. The source descriptor or pointer is retained by the caller; the full program separately checks a zero-length copy and bank restoration.",
    "この断片は`{api}`で{count}バイトをコピーし、バッファーを`{bytes}`と照合します。呼び出し側の転送元記述子やポインターは維持されます。完成プログラムでは、長さ0のコピーとバンク復帰も別に確認します。",
    "이 코드 조각은 `{api}`로 {count}바이트를 복사하고 버퍼를 `{bytes}`와 비교합니다. 호출자의 원본 설명자나 포인터는 유지됩니다. 완성 프로그램은 길이 0 복사와 뱅크 복원도 별도로 확인합니다.",
    "此片段通过 `{api}` 复制 {count} 字节，并将缓冲区与 `{bytes}` 比较。调用方的源描述符或指针保持不变；完整程序还分别检查零长度复制和存储体恢复。",
    "此片段透過 `{api}` 複製 {count} 個位元組，並將緩衝區與 `{bytes}` 比較。呼叫端的來源描述子或指標維持不變；完整程式還分別檢查零長度複製與區塊還原。",
    "Este fragmento copia {count} bytes mediante `{api}` y contrasta el búfer con `{bytes}`. El llamador conserva su descriptor o puntero de origen; el programa completo comprueba aparte una copia de longitud cero y la restauración del banco.",
    "Este trecho copia {count} bytes por `{api}` e compara o buffer com `{bytes}`. O chamador mantém seu descritor ou ponteiro de origem; o programa completo verifica separadamente uma cópia de comprimento zero e a restauração do banco.",
    "Cet extrait copie {count} octets via `{api}` et compare le tampon à `{bytes}`. Le descripteur ou pointeur source de l’appelant est conservé ; le programme complet vérifie séparément une copie de longueur nulle et la restauration de la banque.",
    "Dieser Ausschnitt kopiert mit `{api}` {count} Bytes und prüft den Puffer gegen `{bytes}`. Quelldeskriptor oder Quellzeiger des Aufrufers bleiben erhalten; das vollständige Programm prüft zusätzlich eine Kopie der Länge null und die Bankwiederherstellung."
  ],
  "call": [
    "At this point in the complete program, `{api}` raises `callback_count` to {count}. The callback reads D3 from bank 3. A later near read of B2 from `home2` verifies that execution returned with bank 2 mapped.",
    "完成プログラムのこの位置では、`{api}`によって`callback_count`が{count}になります。コールバックはバンク3からD3を読みます。その後に`home2`を近ポインターで読んでB2を確認し、バンク2が選ばれた状態で復帰したことを確かめます。",
    "완성 프로그램의 이 지점에서 `{api}`는 `callback_count`를 {count}으로 만듭니다. 콜백은 뱅크 3에서 D3을 읽습니다. 이후 `home2`를 근거리로 읽어 B2인지 검사함으로써 뱅크 2가 매핑된 상태로 복귀했는지 확인합니다.",
    "在完整程序的此处，`{api}` 使 `callback_count` 达到 {count}。回调从存储体 3 读取 D3。随后通过近指针读取 `home2` 并确认 B2，验证返回时已恢复存储体 2。",
    "在完整程式的此處，`{api}` 使 `callback_count` 達到 {count}。回呼從區塊 3 讀取 D3。隨後透過近指標讀取 `home2` 並確認 B2，驗證傳回時已還原區塊 2。",
    "En este punto del programa completo, `{api}` eleva `callback_count` a {count}. El callback lee D3 del banco 3. Una lectura cercana posterior de B2 en `home2` verifica el retorno con el banco 2 seleccionado.",
    "Neste ponto do programa completo, `{api}` eleva `callback_count` para {count}. O callback lê D3 do banco 3. Uma leitura próxima posterior de B2 em `home2` verifica o retorno com o banco 2 selecionado.",
    "À ce stade du programme complet, `{api}` porte `callback_count` à {count}. Le rappel lit D3 dans la banque 3. Une lecture proche ultérieure de B2 dans `home2` confirme le retour avec la banque 2 sélectionnée.",
    "An dieser Stelle des vollständigen Programms erhöht `{api}` den Wert von `callback_count` auf {count}. Der Callback liest D3 aus Bank 3. Ein späterer Nahzugriff auf `home2` mit Ergebnis B2 bestätigt die Rückkehr bei eingeblendeter Bank 2."
  ]
}

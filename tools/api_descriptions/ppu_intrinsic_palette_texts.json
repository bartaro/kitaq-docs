{
  "pi_attr": [
    "Write the entire packed attribute byte at `0x23C0 + (y>>2)*8 + (x>>2)` in nametable 0. X/Y are tile coordinates identifying a 4×4-tile attribute area. All four two-bit palette fields are replaced; this does not merge one selected quadrant.",
    "ネームテーブル0の`0x23C0 + (y>>2)*8 + (x>>2)`へ、まとめられた属性1バイト全体を書きます。X・Yはタイル座標で、4×4タイルの属性領域を特定します。2ビットずつの4個のパレット指定をすべて置き換えるため、選んだ四分区画だけを合成する処理ではありません。",
    "네임테이블 0의 `0x23C0 + (y>>2)*8 + (x>>2)`에 압축된 속성 바이트 전체를 씁니다. X/Y는 4×4타일 속성 영역을 가리키는 타일 좌표입니다. 2비트 팔레트 필드 네 개를 모두 바꾸며 선택한 사분면 하나만 병합하지 않습니다.",
    "向名称表 0 的 `0x23C0 + (y>>2)*8 + (x>>2)` 写入整个打包属性字节。X/Y 是图块坐标，用于确定一个 4×4 图块属性区域。四个两位调色板字段都会被替换，不是仅合并选定象限。",
    "向名稱表 0 的 `0x23C0 + (y>>2)*8 + (x>>2)` 寫入整個打包屬性位元組。X/Y 是圖塊座標，用於確定一個 4×4 圖塊屬性區域。四個兩位元調色盤欄位都會被替換，不是僅合併選定象限。",
    "Escribe todo el byte de atributos empaquetados en `0x23C0 + (y>>2)*8 + (x>>2)` de la tabla 0. X/Y son coordenadas de tiles que identifican un área de atributos de 4×4 tiles. Sustituye los cuatro campos de paleta de dos bits; no combina solo un cuadrante seleccionado.",
    "Grava todo o byte de atributos compactados em `0x23C0 + (y>>2)*8 + (x>>2)` da tabela 0. X/Y são coordenadas de tiles que identificam uma área de atributos de 4×4 tiles. Substitui os quatro campos de paleta de dois bits; não combina apenas um quadrante selecionado.",
    "Écrit l’octet complet d’attributs compactés à `0x23C0 + (y>>2)*8 + (x>>2)` dans la table 0. X/Y sont des coordonnées de tuiles désignant une zone d’attributs de 4×4 tuiles. Remplace les quatre champs de palette sur deux bits, sans fusionner uniquement un quadrant choisi.",
    "Schreibt das gesamte gepackte Attributbyte an `0x23C0 + (y>>2)*8 + (x>>2)` in Nametable 0. X/Y sind Kachelkoordinaten, die einen Attributbereich von 4×4 Kacheln bestimmen. Alle vier 2-Bit-Palettenfelder werden ersetzt; es wird nicht nur ein ausgewählter Quadrant eingemischt."
  ],
  "pi_attr_nt": [
    "Select logical nametable `nt & 3`, reset the PPU latch and write one complete attribute byte at `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`. All palette quadrants in that byte are overwritten. Table selection follows the cartridge's current mirroring.",
    "論理ネームテーブル`nt & 3`を選び、PPUラッチをリセットして、`0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`へ属性1バイト全体を書きます。そのバイトのパレット四分区画をすべて上書きします。テーブルの対応はカートリッジの現在のミラーリングに従います。",
    "논리 네임테이블 `nt & 3`을 선택하고 PPU 래치를 초기화한 뒤 `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`에 속성 바이트 전체를 씁니다. 해당 바이트의 모든 팔레트 사분면을 덮어씁니다. 테이블 선택은 카트리지의 현재 미러링을 따릅니다.",
    "选择逻辑名称表 `nt & 3`，复位 PPU 锁存器，并向 `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)` 写入一个完整属性字节。该字节中的所有调色板象限都会被覆盖。表选择遵循卡带当前镜像。",
    "選擇邏輯名稱表 `nt & 3`，重設 PPU 鎖存器，並向 `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)` 寫入一個完整屬性位元組。該位元組中的所有調色盤象限都會被覆寫。表選擇遵循卡匣目前的鏡像。",
    "Selecciona la tabla lógica `nt & 3`, reinicia el latch PPU y escribe un byte completo de atributos en `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`. Sobrescribe todos sus cuadrantes de paleta. La selección sigue el mirroring actual del cartucho.",
    "Seleciona a tabela lógica `nt & 3`, reinicia o latch PPU e grava um byte completo de atributos em `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`. Sobrescreve todos os quadrantes de paleta desse byte. A seleção segue o espelhamento atual do cartucho.",
    "Sélectionne la table logique `nt & 3`, réinitialise le verrou PPU et écrit un octet complet d’attributs à `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`. Écrase tous les quadrants de palette de cet octet. La sélection suit le mirroring actuel de la cartouche.",
    "Wählt die logische Nametable `nt & 3`, setzt den PPU-Latch zurück und schreibt ein vollständiges Attributbyte an `0x23C0 + (nt & 3)*0x400 + (y>>2)*8 + (x>>2)`. Überschreibt alle Palettenquadranten dieses Bytes. Die Tabellenauswahl folgt der aktuellen Cartridge-Spiegelung."
  ],
  "pi_palette_bg": [
    "Reset the PPU latch, set address `0x3F00` and write exactly 16 source bytes through PPUDATA for the four background palettes. This is an immediate transfer; it does not change nametable attribute selections or load tile graphics.",
    "PPUラッチをリセットし、アドレスを`0x3F00`にして、4組の背景パレット用に転送元の16バイトをPPUDATAへ書きます。即時転送であり、ネームテーブルの属性によるパレット選択やタイル画像の読み込みは行いません。",
    "PPU 래치를 초기화하고 주소를 `0x3F00`으로 설정한 뒤 배경 팔레트 네 개를 위해 원본 16바이트를 PPUDATA에 씁니다. 즉시 전송이며 네임테이블 속성의 팔레트 선택을 바꾸거나 타일 그래픽을 불러오지 않습니다.",
    "复位 PPU 锁存器，设置地址 `0x3F00`，通过 PPUDATA 写入源数据的 16 字节，供四组背景调色板使用。这是立即传输，不改变名称表属性中的调色板选择，也不加载图块图形。",
    "重設 PPU 鎖存器，設定位址 `0x3F00`，透過 PPUDATA 寫入來源的 16 位元組，供四組背景調色盤使用。這是立即傳輸，不改變名稱表屬性中的調色盤選擇，也不載入圖塊圖像。",
    "Reinicia el latch PPU, fija `0x3F00` y escribe exactamente 16 bytes del origen mediante PPUDATA para las cuatro paletas de fondo. La transferencia es inmediata; no cambia la selección de paleta de los atributos ni carga gráficos de tiles.",
    "Reinicia o latch PPU, define `0x3F00` e grava exatamente 16 bytes da origem por PPUDATA para as quatro paletas de fundo. A transferência é imediata; não altera a seleção de paleta dos atributos nem carrega gráficos de tiles.",
    "Réinitialise le verrou PPU, définit `0x3F00` et écrit exactement 16 octets de la source via PPUDATA pour les quatre palettes de fond. Le transfert est immédiat ; il ne change pas les sélections de palette des attributs et ne charge pas de graphismes de tuiles.",
    "Setzt den PPU-Latch zurück, setzt die Adresse `0x3F00` und schreibt genau 16 Quellbytes über PPUDATA für die vier Hintergrundpaletten. Die Übertragung erfolgt sofort; weder die Palettenauswahl der Nametable-Attribute noch Kachelgrafiken werden geändert."
  ],
  "pi_palette_sp": [
    "Reset the PPU latch, set address `0x3F10` and write exactly 16 source bytes through PPUDATA for the four sprite palettes. Sprite attribute bits choose which palette is displayed. The call neither modifies OAM nor transfers sprite tile graphics.",
    "PPUラッチをリセットし、アドレスを`0x3F10`にして、4組のスプライトパレット用に転送元の16バイトをPPUDATAへ書きます。表示に使うパレットはスプライト属性のビットで選びます。この呼び出しはOAMの変更やスプライトのタイル画像転送を行いません。",
    "PPU 래치를 초기화하고 주소를 `0x3F10`으로 설정한 뒤 스프라이트 팔레트 네 개를 위해 원본 16바이트를 PPUDATA에 씁니다. 표시할 팔레트는 스프라이트 속성 비트로 선택합니다. OAM을 바꾸거나 스프라이트 타일 그래픽을 전송하지 않습니다.",
    "复位 PPU 锁存器，设置地址 `0x3F10`，通过 PPUDATA 写入源数据的 16 字节，供四组精灵调色板使用。精灵属性位决定显示哪组调色板。此调用不修改 OAM，也不传输精灵图块图形。",
    "重設 PPU 鎖存器，設定位址 `0x3F10`，透過 PPUDATA 寫入來源的 16 位元組，供四組精靈調色盤使用。精靈屬性位元決定顯示哪組調色盤。此呼叫不修改 OAM，也不傳輸精靈圖塊圖像。",
    "Reinicia el latch PPU, fija `0x3F10` y escribe exactamente 16 bytes del origen mediante PPUDATA para las cuatro paletas de sprites. Los bits de atributo del sprite eligen la paleta visible. No modifica OAM ni transfiere gráficos de tiles de sprites.",
    "Reinicia o latch PPU, define `0x3F10` e grava exatamente 16 bytes da origem por PPUDATA para as quatro paletas de sprites. Os bits de atributo do sprite escolhem a paleta exibida. Não modifica OAM nem transfere gráficos de tiles de sprites.",
    "Réinitialise le verrou PPU, définit `0x3F10` et écrit exactement 16 octets de la source via PPUDATA pour les quatre palettes de sprites. Les bits d’attribut du sprite choisissent la palette affichée. Ne modifie pas OAM et ne transfère pas les graphismes de sprites.",
    "Setzt den PPU-Latch zurück, setzt die Adresse `0x3F10` und schreibt genau 16 Quellbytes über PPUDATA für die vier Spritepaletten. Die Sprite-Attributbits wählen die angezeigte Palette. Der Aufruf verändert weder OAM noch überträgt er Sprite-Kachelgrafiken."
  ]
}

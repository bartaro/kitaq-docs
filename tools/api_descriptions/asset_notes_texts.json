{
 "as_registration": [
  "Register the table before looking up resources. Registration is shared global state: do not change it concurrently with a lookup. The library checks IDs and `asset_get`’s output pointer, but does not validate the registered table, payload addresses or cartridge contents.",
  "検索前に一覧を登録してください。登録情報は共有のグローバル状態なので、検索と同時に変更してはいけません。ライブラリはIDと`asset_get`の出力ポインタを確認しますが、登録した一覧や素材のアドレス、カートリッジ内容の正しさまでは検査しません。",
  "조회 전에 목록을 등록하세요. 등록 정보는 공유 전역 상태이므로 조회 중에 바꾸면 안 됩니다. 라이브러리는 ID와 `asset_get`의 출력 포인터를 검사하지만 등록된 목록, 소재 주소, 카트리지 내용의 유효성은 검사하지 않습니다.",
  "查找前请先登记表。登记信息是共享全局状态，不要在查找过程中同时修改。库会检查ID和`asset_get`的输出指针，但不会验证已登记的表、素材地址或卡带内容是否正确。",
  "查找前請先登記表格。登記資訊是共用全域狀態，不要在查找過程中同時修改。函式庫會檢查ID與`asset_get`的輸出指標，但不會驗證已登記的表格、素材位址或卡匣內容是否正確。",
  "Registre la tabla antes de buscar recursos. El registro es un estado global compartido: no lo cambie durante una búsqueda. La biblioteca comprueba los ID y el puntero de salida de `asset_get`, pero no valida la tabla, las direcciones de los datos ni el contenido del cartucho.",
  "Registre a tabela antes de buscar recursos. O registro é um estado global compartilhado: não o altere durante uma busca. A biblioteca verifica os IDs e o ponteiro de saída de `asset_get`, mas não valida a tabela, os endereços dos dados nem o conteúdo do cartucho.",
  "Enregistrez la table avant toute recherche. Cet enregistrement est un état global partagé : ne le modifiez pas pendant une recherche. La bibliothèque vérifie les identifiants et le pointeur de sortie de `asset_get`, mais pas la table enregistrée, les adresses des données ni le contenu de la cartouche.",
  "Registrieren Sie die Tabelle vor der ersten Suche. Die Registrierung ist ein gemeinsamer globaler Zustand und darf nicht gleichzeitig mit einer Suche geändert werden. Geprüft werden IDs und der Ausgabezeiger von `asset_get`, nicht jedoch Tabelle, Nutzdatenadressen oder Cartridge-Inhalt."
 ],
 "as_copy_bounds": [
  "Keep the full source range in the intended ROM mapping and the full destination range in writable memory. Copying does not select the next bank at a window boundary, handle overlapping ranges like `memmove`, or provide VRAM-safe writes. Use the graphics transfer APIs for VRAM.",
  "コピー元全体が目的のROM配置で読め、コピー先全体が書き込み可能な範囲になるようにしてください。バンク窓の端で次のバンクへ進む処理、`memmove`のような重なりへの対応、VRAMの安全な書き込み制御はありません。VRAMには描画用の転送APIを使います。",
  "원본 전체가 의도한 ROM 매핑에서 읽히고 대상 전체가 쓰기 가능한 메모리 안에 있어야 합니다. 뱅크 창 경계에서 다음 뱅크로 넘어가거나, `memmove`처럼 겹치는 범위를 처리하거나, VRAM 쓰기 시점을 제어하지 않습니다. VRAM에는 그래픽 전송 API를 사용하세요.",
  "整个源范围必须在预期ROM映射中可读，整个目标范围必须可写。复制不会在存储体窗口边界自动切换到下一存储体，不提供`memmove`式的重叠处理，也不控制VRAM安全写入时机。写VRAM请使用图形传送API。",
  "整個來源範圍必須在預期ROM映射中可讀取，整個目標範圍必須可寫入。複製不會在區塊視窗邊界自動切換至下一區塊，不提供`memmove`式的重疊處理，也不控制VRAM安全寫入時機。寫入VRAM請使用圖形傳送API。",
  "Mantenga todo el origen en el mapeo de ROM previsto y todo el destino en memoria escribible. La copia no cambia automáticamente de banco al cruzar una ventana, no trata solapamientos como `memmove` ni sincroniza escrituras en VRAM. Para VRAM, use las API de transferencia gráfica.",
  "Mantenha toda a origem no mapeamento de ROM previsto e todo o destino em memória gravável. A cópia não muda automaticamente de banco ao cruzar uma janela, não trata sobreposição como `memmove` nem sincroniza escritas na VRAM. Para VRAM, use as APIs de transferência gráfica.",
  "Toute la source doit être lisible dans le mappage ROM prévu, et toute la destination accessible en écriture. La copie ne passe pas automatiquement à la banque suivante, ne gère pas les chevauchements comme `memmove` et ne synchronise pas les écritures VRAM. Utilisez les API graphiques pour la VRAM.",
  "Der gesamte Quellbereich muss in der vorgesehenen ROM-Zuordnung lesbar und der gesamte Zielbereich beschreibbar sein. Die Kopie schaltet am Fensterende nicht automatisch zur nächsten Bank, behandelt Überlappungen nicht wie `memmove` und steuert keine sicheren VRAM-Schreibzeitpunkte. Verwenden Sie für VRAM die Grafikübertragungs-APIs."
 ],
 "as_fixed_code": [
  "The samples place calling code in fixed bank 0 so source-bank changes cannot unmap the code being executed. These transfers are synchronous. Do not interrupt a transfer with another bank-changing copy or assume the helper makes a whole application interrupt-safe.",
  "サンプルの呼び出し側コードは固定バンク0に配置し、コピー元の切り替えで実行中のコードが見えなくなることを防ぎます。転送は同期処理です。転送中に割り込み側から別のバンク切り替えコピーを実行したり、この関数だけでアプリ全体の割り込み安全性が確保されると考えたりしないでください。",
  "예제의 호출 코드는 고정 뱅크 0에 두어 원본 뱅크를 바꿔도 실행 중인 코드가 사라지지 않도록 합니다. 전송은 동기식입니다. 전송 도중 인터럽트에서 다른 뱅크 전환 복사를 실행하지 마세요. 이 함수만으로 프로그램 전체의 인터럽트 안전성이 확보되지는 않습니다.",
  "示例将调用代码放在固定存储体0中，防止切换源存储体后正在执行的代码不可见。传送为同步操作。不要在传送中通过中断再次执行切换存储体的复制，也不要认为此辅助函数能保证整个程序的中断安全。",
  "範例將呼叫端程式碼放在固定區塊0，避免切換來源區塊後，正在執行的程式碼變得不可見。傳送是同步操作。不要在傳送期間由中斷再次執行切換區塊的複製，也不要認為此輔助函式能確保整個程式的中斷安全。",
  "Los ejemplos sitúan el código llamante en el banco fijo 0 para que cambiar el banco de origen no retire el código en ejecución. Las transferencias son síncronas. No las interrumpa con otra copia que cambie bancos ni suponga que la función garantiza por sí sola la seguridad de todas las interrupciones.",
  "Os exemplos colocam o código chamador no banco fixo 0 para que a troca do banco de origem não remova o código em execução. As transferências são síncronas. Não as interrompa com outra cópia que troque bancos nem suponha que a função, sozinha, torne todo o programa seguro contra interferências de interrupções.",
  "Les exemples placent le code appelant dans la banque fixe 0 afin qu’un changement de banque source ne le rende pas inaccessible. Les transferts sont synchrones. N’interrompez pas un transfert par une autre copie changeant de banque ; cette fonction ne garantit pas à elle seule la sûreté des interruptions de toute l’application.",
  "Die Beispiele legen den aufrufenden Code in die feste Bank 0, damit ein Wechsel der Quellbank den laufenden Code nicht ausblendet. Die Übertragungen sind synchron. Unterbrechen Sie sie nicht durch eine weitere bankwechselnde Kopie. Die Hilfsfunktion allein macht nicht die gesamte Anwendung interruptsicher."
 ],
 "as_gb_bank_state": [
  "The GB helper restores the compiler’s ROM-bank shadow. Use `__bankswitch` to maintain it; raw writes to the mapper do not update that shadow. Its saved-bank slot in HRAM is shared, so nested transfers that overwrite it are unsafe.",
  "GB版の補助処理は、コンパイラが保持するROMバンク情報から復帰先を決めます。その情報を更新する`__bankswitch`を使ってください。マッパーへの直接書き込みでは更新されません。HRAMの復帰バンク保存領域は共有なので、入れ子の転送で上書きする使い方は安全ではありません。",
  "GB 보조 루틴은 컴파일러가 보관하는 ROM 뱅크 상태로 복귀 뱅크를 정합니다. 이를 갱신하는 `__bankswitch`를 사용하세요. 매퍼에 직접 쓰면 상태가 갱신되지 않습니다. HRAM의 복귀 뱅크 저장 공간은 공유되므로 중첩 전송으로 덮어쓰면 안전하지 않습니다.",
  "GB辅助例程根据编译器保存的ROM存储体状态恢复映射。请用`__bankswitch`维护该状态；直接写映射器不会更新它。HRAM中的返回存储体保存位置是共享的，嵌套传送覆盖此位置会造成风险。",
  "GB輔助常式依編譯器保存的ROM區塊狀態恢復映射。請用`__bankswitch`維護此狀態；直接寫入映射器不會更新它。HRAM中的返回區塊儲存位置是共用的，巢狀傳送若覆寫此位置就不安全。",
  "La rutina de GB restaura el banco registrado por el compilador. Manténgalo actualizado con `__bankswitch`; escribir directamente en el mapper no lo actualiza. La posición de HRAM que guarda el banco de retorno es compartida, por lo que no admite transferencias anidadas que la sobrescriban.",
  "A rotina do GB restaura o banco registrado pelo compilador. Mantenha esse estado atualizado com `__bankswitch`; escrever diretamente no mapper não o atualiza. A posição da HRAM que guarda o banco de retorno é compartilhada, portanto transferências aninhadas que a sobrescrevam não são seguras.",
  "La routine GB rétablit la banque mémorisée par le compilateur. Maintenez cet état avec `__bankswitch` : une écriture directe dans le mapper ne le met pas à jour. L’emplacement HRAM de sauvegarde est partagé ; des transferts imbriqués qui l’écrasent ne sont pas sûrs.",
  "Die GB-Hilfsroutine stellt die vom Compiler gemerkte ROM-Bank wieder her. Halten Sie diesen Zustand mit `__bankswitch` aktuell; direkte Mapper-Schreibzugriffe aktualisieren ihn nicht. Der HRAM-Speicherplatz für die Rückkehrbank wird gemeinsam genutzt. Verschachtelte Übertragungen dürfen ihn nicht überschreiben."
 ],
 "as_fc_bank_state": [
  "`asset_load_tiles` restores `bank.c`’s software-tracked bank, not a mapper readback. Initialize and maintain that state with `bank_switch`; direct mapper writes or calls to `__bankswitch` alone do not update the library’s state. The far-copy intrinsic separately restores the compiler’s bank state.",
  "`asset_load_tiles`の復帰先は、マッパーから読み取った値ではなく`bank.c`が記録した値です。`bank_switch`で初期化・更新してください。マッパーへの直接書き込みや`__bankswitch`だけの呼び出しでは、ライブラリ側の記録は更新されません。バンク間コピーの組み込み関数は、これとは別にコンパイラ側のバンク状態を復帰します。",
  "`asset_load_tiles`는 매퍼에서 읽은 값이 아니라 `bank.c`가 기록한 뱅크로 돌아갑니다. `bank_switch`로 상태를 초기화하고 유지하세요. 매퍼에 직접 쓰거나 `__bankswitch`만 호출하면 라이브러리 상태는 갱신되지 않습니다. 뱅크 간 복사 내장 함수는 별도로 컴파일러의 뱅크 상태를 복원합니다.",
  "`asset_load_tiles`恢复的是`bank.c`记录的存储体，并非从映射器读回的值。请用`bank_switch`初始化并维护状态；直接写映射器或仅调用`__bankswitch`不会更新库的记录。跨存储体复制内置函数则单独恢复编译器保存的状态。",
  "`asset_load_tiles`恢復的是`bank.c`記錄的區塊，而非從映射器讀回的值。請用`bank_switch`初始化並維護狀態；直接寫入映射器或僅呼叫`__bankswitch`不會更新函式庫的記錄。跨區塊複製內建函式則另外恢復編譯器保存的狀態。",
  "`asset_load_tiles` restaura el banco registrado por `bank.c`, no un valor leído del mapper. Inicialice y mantenga ese estado con `bank_switch`; las escrituras directas y las llamadas aisladas a `__bankswitch` no actualizan el registro de la biblioteca. La copia intrínseca restaura por separado el estado del compilador.",
  "`asset_load_tiles` restaura o banco registrado por `bank.c`, não um valor lido do mapper. Inicialize e mantenha esse estado com `bank_switch`; escritas diretas e chamadas isoladas a `__bankswitch` não atualizam o registro da biblioteca. A cópia intrínseca restaura separadamente o estado do compilador.",
  "`asset_load_tiles` rétablit la banque suivie par `bank.c`, sans relire le mapper. Initialisez et maintenez cet état avec `bank_switch` ; les écritures directes et les appels isolés à `__bankswitch` ne mettent pas à jour la bibliothèque. La copie intrinsèque rétablit séparément l’état du compilateur.",
  "`asset_load_tiles` stellt die von `bank.c` vermerkte Bank wieder her, keinen vom Mapper zurückgelesenen Wert. Initialisieren und pflegen Sie diesen Zustand mit `bank_switch`. Direkte Mapper-Zugriffe oder alleinige Aufrufe von `__bankswitch` aktualisieren den Bibliothekszustand nicht. Die Kopier-Intrinsic stellt separat den Compilerzustand wieder her."
 ],
 "as_tile_timing": [
  "The upload only writes bytes; it does not place tiles on the map, assign colors or allocate VRAM. Arrange the tile map and palettes separately. The samples upload with rendering disabled and enable the display only after the image is ready.",
  "転送はバイトを書き込むだけで、マップへのタイル配置、配色、VRAMの割り当ては行いません。タイルマップとパレットは別途設定します。サンプルでは描画を止めて転送し、画像の準備ができてから表示を有効にします。",
  "전송은 바이트만 쓰며 맵에 타일을 배치하거나 색을 지정하거나 VRAM을 할당하지 않습니다. 타일 맵과 팔레트는 따로 설정하세요. 예제는 렌더링을 끈 상태로 전송하고 이미지가 준비된 뒤 화면을 켭니다.",
  "上传只写入字节，不会在地图上放置图块、设置颜色或分配VRAM。图块地图与调色板需要另行设置。示例在关闭渲染时上传，准备好图像后才启用显示。",
  "上傳只寫入位元組，不會在地圖上放置圖塊、設定顏色或配置VRAM。圖塊地圖與調色盤需要另外設定。範例會關閉繪製後上傳，準備好圖像才啟用顯示。",
  "La transferencia solo escribe bytes: no coloca tiles en el mapa, asigna colores ni reserva VRAM. Configure el mapa y las paletas por separado. Los ejemplos transfieren con el renderizado desactivado y activan la imagen cuando está lista.",
  "A transferência apenas escreve bytes: não posiciona tiles no mapa, não atribui cores nem aloca VRAM. Configure o mapa e as paletas separadamente. Os exemplos transferem com a renderização desativada e habilitam a exibição quando a imagem está pronta.",
  "Le transfert écrit uniquement des octets : il ne place pas les tuiles sur la carte, n’attribue pas de couleurs et n’alloue pas de VRAM. Configurez séparément la carte et les palettes. Les exemples effectuent le transfert avec le rendu désactivé, puis activent l’affichage.",
  "Der Transfer schreibt nur Bytes. Er platziert keine Kacheln auf der Karte, weist keine Farben zu und reserviert keinen VRAM. Kachelkarte und Paletten werden separat eingerichtet. Die Beispiele übertragen bei abgeschalteter Darstellung und schalten sie erst nach dem Bildaufbau ein."
 ],
 "as_fc_timing": [
  "Use writable CHR RAM for pattern data and keep rendering off for long uploads. If uploading in VBlank, the complete operation must fit the available time; 128-byte chunks do not enforce that limit. This sample uses `--mapper=mmc1 --board=surom512`. With generic boards, omitting `--nes-chr` creates blank CHR ROM, not CHR RAM.",
  "絵柄データには書き込み可能なCHR RAMを使い、大きな転送では描画を停止します。VBlank中に転送するなら処理全体がその時間内に収まる必要があり、128バイト分割だけでは時間制限を守れません。このサンプルは`--mapper=mmc1 --board=surom512`でビルドします。汎用ボードで`--nes-chr`を省略しても、作られるのは空のCHR ROMで、CHR RAMにはなりません。",
  "패턴 데이터에는 쓰기 가능한 CHR RAM을 사용하고 긴 전송 동안은 렌더링을 끄세요. VBlank에서 전송한다면 전체 작업이 허용 시간 안에 끝나야 하며 128바이트 분할만으로 이를 보장하지 않습니다. 예제는 `--mapper=mmc1 --board=surom512`로 빌드합니다. 일반 보드에서 `--nes-chr`를 생략하면 CHR RAM이 아니라 빈 CHR ROM이 만들어집니다.",
  "图案数据须使用可写CHR RAM，大量上传时应关闭渲染。若在VBlank期间上传，整个操作必须在可用时间内完成；每次128字节并不会自动保证这一点。示例使用`--mapper=mmc1 --board=surom512`。通用板型省略`--nes-chr`会生成空CHR ROM，而不是CHR RAM。",
  "圖案資料須使用可寫入的CHR RAM，大量上傳時應關閉繪製。若在VBlank期間上傳，整個操作必須在可用時間內完成；每次128位元組並不會自動保證這一點。範例使用`--mapper=mmc1 --board=surom512`。一般板型省略`--nes-chr`會產生空白CHR ROM，而不是CHR RAM。",
  "Use CHR RAM escribible para los patrones y desactive el renderizado en transferencias largas. En VBlank, la operación completa debe caber en el tiempo disponible; los bloques de 128 bytes no lo garantizan. El ejemplo usa `--mapper=mmc1 --board=surom512`. En placas genéricas, omitir `--nes-chr` genera CHR ROM vacía, no CHR RAM.",
  "Use CHR RAM gravável para os padrões e desative a renderização em transferências longas. Em VBlank, a operação inteira precisa caber no tempo disponível; blocos de 128 bytes não garantem isso. O exemplo usa `--mapper=mmc1 --board=surom512`. Em placas genéricas, omitir `--nes-chr` gera uma CHR ROM vazia, não CHR RAM.",
  "Utilisez de la CHR RAM accessible en écriture et désactivez le rendu pour les longs transferts. Pendant VBlank, toute l’opération doit tenir dans le temps disponible ; les blocs de 128 octets ne le garantissent pas. L’exemple utilise `--mapper=mmc1 --board=surom512`. Sur les cartes génériques, omettre `--nes-chr` produit une CHR ROM vide, pas de la CHR RAM.",
  "Verwenden Sie beschreibbaren CHR RAM für Muster und schalten Sie die Darstellung bei langen Transfers ab. Innerhalb von VBlank muss der gesamte Vorgang in die verfügbare Zeit passen; 128-Byte-Blöcke gewährleisten das nicht. Das Beispiel nutzt `--mapper=mmc1 --board=surom512`. Bei generischen Boards erzeugt das Weglassen von `--nes-chr` leeren CHR ROM, keinen CHR RAM."
 ],
 "as_lookup_cost": [
  "Each getter calls `asset_get` again and copies a descriptor. If you need bank, pointer and length together, call `asset_get` once and read its fields.",
  "各取得関数はその都度`asset_get`を呼び、記述子をコピーします。バンク・ポインタ・長さをまとめて使うなら、`asset_get`を1回呼んで各フィールドを参照します。",
  "각 조회 함수는 호출할 때마다 `asset_get`으로 설명자를 복사합니다. 뱅크, 포인터, 길이가 모두 필요하다면 `asset_get`을 한 번 호출하고 필드를 읽으세요.",
  "每个获取函数都会再次调用`asset_get`并复制描述符。若同时需要存储体、指针和长度，请只调用一次`asset_get`后读取各字段。",
  "每個取得函式都會再次呼叫`asset_get`並複製描述符。若同時需要區塊、指標與長度，請只呼叫一次`asset_get`後讀取各欄位。",
  "Cada consulta llama de nuevo a `asset_get` y copia un descriptor. Si necesita banco, puntero y longitud, llame una sola vez a `asset_get` y lea sus campos.",
  "Cada consulta chama `asset_get` novamente e copia um descritor. Se precisar de banco, ponteiro e comprimento, chame `asset_get` uma vez e leia seus campos.",
  "Chaque accesseur rappelle `asset_get` et copie un descripteur. Pour obtenir banque, pointeur et longueur ensemble, appelez `asset_get` une seule fois et lisez ses champs.",
  "Jede Abfrage ruft erneut `asset_get` auf und kopiert einen Deskriptor. Wenn Bank, Zeiger und Länge gemeinsam benötigt werden, genügt ein Aufruf von `asset_get` mit anschließendem Lesen der Felder."
 ]
}

{
  "pi_byte": [
    "Unsigned byte `0..255`, written as supplied. This is a complete register/data byte, not a Boolean or a bit number.",
    "指定どおりに書く符号なし1バイト`0..255`です。真偽値やビット番号ではなく、レジスターまたはデータの1バイト全体を渡します。",
    "지정한 그대로 쓰는 부호 없는 바이트 `0..255`입니다. 불리언이나 비트 번호가 아니라 레지스터 또는 데이터 바이트 전체입니다.",
    "无符号字节 `0..255`，按原值写入。这是完整的寄存器或数据字节，不是布尔值或位编号。",
    "無號位元組 `0..255`，依原值寫入。這是完整的暫存器或資料位元組，不是布林值或位元編號。",
    "Byte sin signo `0..255`, escrito tal como se recibe. Es un byte completo de registro o datos, no un booleano ni un número de bit.",
    "Byte sem sinal `0..255`, gravado como recebido. É um byte completo de registrador ou dados, não um booleano ou número de bit.",
    "Octet non signé de `0..255`, écrit tel quel. C’est un octet complet de registre ou de données, pas un booléen ni un numéro de bit.",
    "Vorzeichenloses Byte `0..255`, das unverändert geschrieben wird. Gemeint ist ein vollständiges Register- oder Datenbyte, kein Wahrheitswert und keine Bitnummer."
  ],
  "pi_length": [
    "Number of bytes, `0..255`. Zero still sets the PPU address and resets the latch, but writes no data. There is no destination-capacity check, clipping, bank switch or deferred transfer.",
    "バイト数`0..255`です。0でもPPUアドレス設定とラッチのリセットは行いますが、データは書きません。転送先の容量確認、切り詰め、バンク切り替え、遅延転送はありません。",
    "바이트 수 `0..255`입니다. 0이어도 PPU 주소와 래치를 설정하지만 데이터는 쓰지 않습니다. 대상 용량 검사, 경계 잘라내기, 뱅크 전환, 지연 전송은 없습니다.",
    "字节数 `0..255`。0 仍会设置 PPU 地址并复位锁存器，但不写数据。不检查目标容量、不裁剪、不切换存储体，也不延后传输。",
    "位元組數 `0..255`。0 仍會設定 PPU 位址並重設鎖存器，但不寫資料。不檢查目的地容量、不裁切、不切換記憶體庫，也不延後傳輸。",
    "Número de bytes `0..255`. Cero también fija la dirección PPU y reinicia el latch, pero no escribe datos. No hay comprobación de capacidad, recorte, cambio de banco ni transferencia diferida.",
    "Quantidade de bytes `0..255`. Zero também define o endereço PPU e reinicia o latch, mas não grava dados. Não há verificação de capacidade, limitação da área, troca de banco ou transferência adiada.",
    "Nombre d’octets de `0..255`. Zéro définit aussi l’adresse PPU et réinitialise le verrou, sans écrire de données. Aucun contrôle de capacité, découpage, changement de banque ou transfert différé n’est prévu.",
    "Byteanzahl `0..255`. Auch null setzt die PPU-Adresse und den Latch zurück, schreibt aber keine Daten. Zielkapazität und Grenzen werden nicht geprüft; Bankwechsel und verzögerte Übertragung sind nicht enthalten."
  ],
  "pi_xy": [
    "Tile coordinates, not pixels. Keep `x` in `0..31` and `y` in `0..29` for the visible 32×30 tile map. Each tile covers 8×8 pixels. The byte arguments are not clamped; invalid coordinates can write outside the intended tile area.",
    "ピクセルではなくタイル座標です。32×30タイルの表示領域では`x`を`0..31`、`y`を`0..29`に収めます。1タイルは8×8ピクセルです。1バイトの引数を範囲内へ補正しないため、範囲外の座標では意図したタイル領域の外へ書く場合があります。",
    "픽셀이 아닌 타일 좌표입니다. 화면의 32×30타일 영역에서는 `x`를 `0..31`, `y`를 `0..29`로 유지하세요. 한 타일은 8×8픽셀입니다. 바이트 인수를 제한하지 않으므로 범위 밖 좌표는 의도한 타일 영역 밖에 쓸 수 있습니다.",
    "图块坐标，不是像素坐标。可见的 32×30 图块地图应使用 `x=0..31`、`y=0..29`；每个图块为 8×8 像素。字节参数不会被限制在范围内，无效坐标可能写到预期图块区域之外。",
    "圖塊座標，不是像素座標。可見的 32×30 圖塊地圖應使用 `x=0..31`、`y=0..29`；每個圖塊為 8×8 像素。位元組參數不會被限制在範圍內，無效座標可能寫到預期圖塊區域之外。",
    "Coordenadas de tiles, no píxeles. Use `x=0..31` e `y=0..29` para el mapa visible de 32×30 tiles; cada tile ocupa 8×8 píxeles. Los argumentos de un byte no se limitan automáticamente y pueden escribir fuera del área prevista.",
    "Coordenadas de tiles, não pixels. Use `x=0..31` e `y=0..29` para o mapa visível de 32×30 tiles; cada tile ocupa 8×8 pixels. Os argumentos de um byte não são limitados automaticamente e podem escrever fora da área desejada.",
    "Coordonnées de tuiles, pas de pixels. Gardez `x` dans `0..31` et `y` dans `0..29` pour la carte visible de 32×30 tuiles ; chaque tuile couvre 8×8 pixels. Les arguments d’un octet ne sont pas bornés automatiquement et peuvent écrire hors de la zone voulue.",
    "Kachelkoordinaten, keine Pixel. Verwenden Sie `x=0..31` und `y=0..29` für die sichtbare Karte von 32×30 Kacheln; jede Kachel umfasst 8×8 Pixel. Die Byteargumente werden nicht begrenzt und können außerhalb des vorgesehenen Kachelbereichs schreiben."
  ],
  "pi_extent": [
    "Width and height in tiles, each an unsigned byte. Keep `x+w<=32` and `y+h<=30`. Height zero performs no PPU accesses. Width zero writes no tile bytes, but still resets the latch and seeks each of the `h` row addresses. Use PPU increment 1 for horizontal rows.",
    "タイル単位の幅と高さで、どちらも符号なし1バイトです。`x+w<=32`、`y+h<=30`に収めてください。高さ0ならPPUにアクセスしません。幅0ならタイル番号は書きませんが、`h`行それぞれのラッチリセットとアドレス設定は行います。横に並ぶ行にはPPU増分1を使います。",
    "타일 단위 너비와 높이이며 각각 부호 없는 바이트입니다. `x+w<=32`, `y+h<=30`을 지키세요. 높이 0은 PPU에 접근하지 않습니다. 너비 0은 타일을 쓰지 않지만 `h`개 행의 래치 초기화와 주소 설정은 수행합니다. 가로 행에는 PPU 증가량 1을 사용하세요.",
    "以图块为单位的宽度和高度，各为无符号字节。请保持 `x+w<=32`、`y+h<=30`。高度为 0 时不访问 PPU；宽度为 0 时不写图块字节，但仍逐行复位锁存器并设置 `h` 个行地址。水平行应使用 PPU 增量 1。",
    "以圖塊為單位的寬度和高度，各為無號位元組。請保持 `x+w<=32`、`y+h<=30`。高度為 0 時不存取 PPU；寬度為 0 時不寫圖塊位元組，但仍逐行重設鎖存器並設定 `h` 個行位址。水平行應使用 PPU 增量 1。",
    "Anchura y altura en tiles, cada una un byte sin signo. Mantenga `x+w<=32` e `y+h<=30`. Altura cero no accede a la PPU. Anchura cero no escribe tiles, pero reinicia el latch y fija las `h` direcciones de fila. Use incremento PPU 1 para filas horizontales.",
    "Largura e altura em tiles, cada uma um byte sem sinal. Mantenha `x+w<=32` e `y+h<=30`. Altura zero não acessa a PPU. Largura zero não grava tiles, mas reinicia o latch e define os `h` endereços de linha. Use incremento PPU 1 para linhas horizontais.",
    "Largeur et hauteur en tuiles, chacune sur un octet non signé. Respectez `x+w<=32` et `y+h<=30`. Une hauteur nulle n’accède pas à la PPU. Une largeur nulle n’écrit aucune tuile, mais réinitialise le verrou et définit les `h` adresses de ligne. Utilisez l’incrément PPU 1 pour les lignes horizontales.",
    "Breite und Höhe in Kacheln, jeweils ein vorzeichenloses Byte. Halten Sie `x+w<=32` und `y+h<=30` ein. Höhe null führt keine PPU-Zugriffe aus. Breite null schreibt keine Kachelbytes, setzt aber den Latch zurück und setzt alle `h` Zeilenadressen. Verwenden Sie für horizontale Zeilen PPU-Schrittweite 1."
  ],
  "pi_nt": [
    "Logical nametable selector. Only the lowest two bits are used (`nt & 3`); for example, 4 selects table 0 and 7 selects table 3. This does not allocate independent storage or change mapper mirroring.",
    "論理ネームテーブルの選択値です。下位2ビットだけを使うため（`nt & 3`）、たとえば4はテーブル0、7はテーブル3を選びます。独立した保存領域の確保やマッパーのミラーリング変更は行いません。",
    "논리 네임테이블 선택값입니다. 하위 두 비트만 사용하므로(`nt & 3`) 4는 테이블 0, 7은 테이블 3을 선택합니다. 독립 저장 공간을 확보하거나 매퍼 미러링을 바꾸지 않습니다.",
    "逻辑名称表选择值，只使用最低两位（`nt & 3`）。例如 4 选择表 0，7 选择表 3。不会分配独立存储空间，也不改变映射器镜像。",
    "邏輯名稱表選擇值，只使用最低兩個位元（`nt & 3`）。例如 4 選擇表 0，7 選擇表 3。不會配置獨立儲存空間，也不改變映射器鏡像。",
    "Selector de tabla lógica. Solo usa los dos bits inferiores (`nt & 3`): 4 selecciona la tabla 0 y 7 la tabla 3. No asigna almacenamiento independiente ni cambia el mirroring del mapper.",
    "Seletor de tabela lógica. Usa apenas os dois bits inferiores (`nt & 3`): 4 seleciona a tabela 0 e 7, a tabela 3. Não aloca armazenamento independente nem altera o espelhamento do mapper.",
    "Sélecteur de table logique. Seuls les deux bits faibles sont utilisés (`nt & 3`) : 4 choisit la table 0 et 7 la table 3. N’alloue pas de mémoire indépendante et ne change pas le mirroring du mapper.",
    "Auswahl der logischen Nametable. Nur die unteren zwei Bits zählen (`nt & 3`): 4 wählt Tabelle 0, 7 wählt Tabelle 3. Es wird kein unabhängiger Speicher reserviert und keine Mapper-Spiegelung geändert."
  ],
  "pi_tile": [
    "Tile number `0..255` in the background pattern table selected by PPUCTRL. It selects existing CHR graphics; it is not a pixel color or a source-data pointer.",
    "PPUCTRLで選んだ背景パターンテーブル内のタイル番号`0..255`です。既存のCHR画像を選ぶ値で、ピクセルの色や転送元ポインターではありません。",
    "PPUCTRL이 선택한 배경 패턴 테이블의 타일 번호 `0..255`입니다. 기존 CHR 그래픽을 선택하는 값이며 픽셀 색상이나 원본 포인터가 아닙니다.",
    "PPUCTRL 所选背景图案表中的图块编号 `0..255`。它选择已有 CHR 图形，不是像素颜色或源数据指针。",
    "PPUCTRL 所選背景圖樣表中的圖塊編號 `0..255`。它選擇既有 CHR 圖像，不是像素色彩或來源指標。",
    "Número de tile `0..255` en la tabla de patrones de fondo seleccionada por PPUCTRL. Selecciona gráficos CHR existentes, no un color de píxel ni un puntero de origen.",
    "Número de tile `0..255` na tabela de padrões de fundo selecionada por PPUCTRL. Seleciona gráficos CHR existentes, não uma cor de pixel ou ponteiro de origem.",
    "Numéro de tuile `0..255` dans la table de motifs de fond choisie par PPUCTRL. Sélectionne un graphisme CHR existant, pas une couleur de pixel ni un pointeur source.",
    "Kachelnummer `0..255` in der durch PPUCTRL gewählten Hintergrund-Mustertabelle. Wählt vorhandene CHR-Grafiken aus, keine Pixelfarbe und keinen Quellzeiger."
  ]
}

{
  "vm_surface": [
    "These macros use the compiler’s intrinsic command queue, also used by `lib/vram.c`. They do not select the C runtime’s `nes_vram_queue_*` buffer. Inspect pending encoded bytes with `__vramq_len()` and capacity failures with `__vramq_overflow()`; use `__vramq_clear_overflow()` to acknowledge the failure flag.",
    "これらのマクロは、`lib/vram.c`も使うコンパイラの組み込みコマンドキューを操作します。Cランタイムの`nes_vram_queue_*`用バッファを選ぶものではありません。保留中の符号化済みバイト数は`__vramq_len()`、容量不足は`__vramq_overflow()`で調べ、失敗フラグは`__vramq_clear_overflow()`で解除します。",
    "이 매크로들은 `lib/vram.c`도 사용하는 컴파일러 내장 명령 큐를 조작합니다. C 런타임의 `nes_vram_queue_*` 버퍼를 선택하지는 않습니다. 대기 중인 인코딩 바이트 수는 `__vramq_len()`, 용량 부족은 `__vramq_overflow()`로 확인하고, 실패 플래그는 `__vramq_clear_overflow()`로 해제하세요.",
    "这些宏操作编译器的内置命令队列，`lib/vram.c` 也使用同一队列。它们不会选择 C 运行时的 `nes_vram_queue_*` 缓冲区。用 `__vramq_len()` 查询待处理命令的编码字节数，用 `__vramq_overflow()` 检查容量不足，再用 `__vramq_clear_overflow()` 清除失败标志。",
    "這些巨集操作編譯器的內建命令佇列，`lib/vram.c` 也使用同一佇列。它們不會選用 C 執行階段的 `nes_vram_queue_*` 緩衝區。用 `__vramq_len()` 查詢待處理命令的編碼位元組數，用 `__vramq_overflow()` 檢查容量不足，再用 `__vramq_clear_overflow()` 清除失敗旗標。",
    "Estas macros usan la cola de comandos intrínseca del compilador, compartida con `lib/vram.c`. No seleccionan el búfer C `nes_vram_queue_*`. Consulte los bytes codificados pendientes con `__vramq_len()` y los fallos de capacidad con `__vramq_overflow()`; borre la marca de fallo con `__vramq_clear_overflow()`.",
    "Essas macros usam a fila de comandos intrínseca do compilador, compartilhada com `lib/vram.c`. Elas não selecionam o buffer C `nes_vram_queue_*`. Consulte os bytes codificados pendentes com `__vramq_len()` e as falhas de capacidade com `__vramq_overflow()`; limpe a indicação de falha com `__vramq_clear_overflow()`.",
    "Ces macros utilisent la file de commandes intrinsèque du compilateur, également employée par `lib/vram.c`. Elles ne sélectionnent pas le tampon C `nes_vram_queue_*`. Consultez les octets encodés en attente avec `__vramq_len()` et les dépassements de capacité avec `__vramq_overflow()` ; acquittez le drapeau d’échec avec `__vramq_clear_overflow()`.",
    "Diese Makros verwenden die Intrinsic-Befehlswarteschlange des Compilers, die auch `lib/vram.c` nutzt. Sie wählen nicht den C-Laufzeitpuffer `nes_vram_queue_*`. Mit `__vramq_len()` lesen Sie die Zahl ausstehender codierter Bytes, mit `__vramq_overflow()` erkennen Sie Kapazitätsfehler; `__vramq_clear_overflow()` löscht das Fehlerflag."
  ],
  "vm_example": [
    "The complete program uses `nes_game.h` macros for put, copy, fill, commit and queue clearing. It also uses `__vramq_*` inspection and execution intrinsics to check their effects. The title is VRAM QUEUE MACROS. The large transfers execute with rendering disabled; committing a large transfer does not make it fit within VBlank.",
    "完成サンプルは、1バイト追加・コピー・塗りつぶし・コミット・キュー消去に`nes_game.h`のマクロを使います。効果の確認には`__vramq_*`の照会・実行用組み込み関数も使います。画面タイトルはVRAM QUEUE MACROSです。大きな転送は描画を無効にして実行します。コミットするだけで大きな転送がVBlank内に収まるようになるわけではありません。",
    "완성 예제는 1바이트 추가, 복사, 채우기, 커밋, 큐 비우기에 `nes_game.h` 매크로를 사용합니다. 효과를 확인하기 위해 `__vramq_*` 조회 및 실행 내장 함수도 사용합니다. 화면 제목은 VRAM QUEUE MACROS입니다. 큰 전송은 렌더링을 끈 상태에서 실행합니다. 커밋한다고 해서 큰 전송이 VBlank 안에 끝나는 것은 아닙니다.",
    "完整示例使用 `nes_game.h` 中的宏完成单字节入队、复制、填充、提交和清空队列，并使用 `__vramq_*` 查询及执行内置函数检查效果。画面标题为 VRAM QUEUE MACROS。大批量传送在关闭渲染时执行；提交操作本身并不能保证大批量传送在 VBlank 内完成。",
    "完整範例使用 `nes_game.h` 中的巨集完成單一位元組入列、複製、填滿、提交及清空佇列，並使用 `__vramq_*` 查詢與執行內建函式檢查效果。畫面標題為 VRAM QUEUE MACROS。大量傳送在關閉繪製時執行；提交操作本身無法保證大量傳送在 VBlank 內完成。",
    "El programa completo usa macros de `nes_game.h` para añadir un byte, copiar, rellenar, confirmar y vaciar la cola. También utiliza los intrínsecos de consulta y ejecución `__vramq_*` para comprobar sus efectos. El título es VRAM QUEUE MACROS. Las transferencias grandes se ejecutan con el renderizado desactivado; confirmarlas no garantiza que quepan en VBlank.",
    "O programa completo usa macros de `nes_game.h` para adicionar um byte, copiar, preencher, confirmar e limpar a fila. Também utiliza as funções intrínsecas de consulta e execução `__vramq_*` para verificar os efeitos. O título é VRAM QUEUE MACROS. As transferências grandes são executadas com a renderização desativada; confirmá-las não garante que caibam no VBlank.",
    "Le programme complet utilise les macros de `nes_game.h` pour ajouter un octet, copier, remplir, valider et vider la file. Il emploie aussi les intrinsèques de consultation et d’exécution `__vramq_*` pour vérifier leurs effets. Le titre est VRAM QUEUE MACROS. Les gros transferts s’exécutent avec le rendu désactivé ; les valider ne garantit pas qu’ils tiennent dans VBlank.",
    "Das vollständige Programm verwendet Makros aus `nes_game.h` zum Einreihen einzelner Bytes, Kopieren, Füllen, Freigeben und Leeren der Warteschlange. Zusätzlich prüfen die Abfrage- und Ausführungs-Intrinsics `__vramq_*` deren Wirkung. Der Titel lautet VRAM QUEUE MACROS. Große Übertragungen laufen bei abgeschalteter Bildausgabe; ihre Freigabe garantiert nicht, dass sie innerhalb von VBlank abgeschlossen werden."
  ],
  "vm_module": [
    "`nes_game.h` provides readable macro names for VRAM queues, OAM and metasprites, controller reads, mapper interrupts, split scrolling and FDS operations. Each macro expands to a compiler intrinsic so diagnostics and KUROSAKI metadata retain the underlying hardware operation. Its queue macros use the intrinsic queue; its OAM DMA macro has no arguments.",
    "`nes_game.h`は、VRAMキュー、OAM・メタスプライト、コントローラー読み取り、マッパー割り込み、分割スクロール、FDS操作に読みやすいマクロ名を提供します。各マクロはコンパイラの組み込み関数へ展開されるため、診断やKUROSAKIメタデータにも実際のハードウェア操作が記録されます。キュー操作には組み込みキューを使い、OAM DMAマクロは引数を取りません。",
    "`nes_game.h`는 VRAM 큐, OAM과 메타스프라이트, 컨트롤러 읽기, 매퍼 인터럽트, 분할 스크롤, FDS 연산에 읽기 쉬운 매크로 이름을 제공합니다. 각 매크로는 컴파일러 내장 함수로 확장되므로 진단과 KUROSAKI 메타데이터에도 실제 하드웨어 연산이 기록됩니다. 큐 매크로는 내장 큐를 사용하며 OAM DMA 매크로는 인수가 없습니다.",
    "`nes_game.h` 为 VRAM 队列、OAM 与组合精灵、控制器读取、映射器中断、分区滚动和 FDS 操作提供易读的宏名称。每个宏都会展开为编译器内置函数，因此诊断信息和 KUROSAKI 元数据仍能记录底层硬件操作。队列宏使用内置队列，OAM DMA 宏不接收参数。",
    "`nes_game.h` 為 VRAM 佇列、OAM 與組合精靈、控制器讀取、映射器中斷、分割捲動和 FDS 操作提供易讀的巨集名稱。每個巨集都會展開為編譯器內建函式，因此診斷資訊和 KUROSAKI 中繼資料仍能記錄底層硬體操作。佇列巨集使用內建佇列，OAM DMA 巨集不接收參數。",
    "`nes_game.h` ofrece nombres de macro legibles para colas VRAM, OAM y metasprites, mandos, interrupciones del mapper, desplazamiento dividido y operaciones FDS. Cada macro se expande a un intrínseco, por lo que los diagnósticos y los metadatos de KUROSAKI conservan la operación de hardware subyacente. Las macros de cola usan la cola intrínseca; la macro DMA de OAM no recibe argumentos.",
    "`nes_game.h` oferece nomes de macro legíveis para filas de VRAM, OAM e metasprites, controles, interrupções do mapper, rolagem dividida e operações FDS. Cada macro expande para uma função intrínseca, mantendo a operação de hardware nos diagnósticos e metadados do KUROSAKI. As macros de fila usam a fila intrínseca; a macro DMA de OAM não recebe argumentos.",
    "`nes_game.h` fournit des noms de macros lisibles pour les files VRAM, l’OAM et les métasprites, les manettes, les interruptions du mapper, le défilement scindé et les opérations FDS. Chaque macro se développe en un intrinsèque : les diagnostics et les métadonnées KUROSAKI conservent donc l’opération matérielle sous-jacente. Les macros de file utilisent la file intrinsèque ; la macro DMA de l’OAM n’a pas d’argument.",
    "`nes_game.h` bietet lesbare Makronamen für VRAM-Warteschlangen, OAM und Metasprites, Controller-Abfragen, Mapper-Interrupts, geteiltes Scrolling und FDS-Operationen. Jedes Makro expandiert zu einem Compiler-Intrinsic, sodass Diagnosemeldungen und KUROSAKI-Metadaten die zugrunde liegende Hardwareoperation erfassen. Die Warteschlangenmakros nutzen die Intrinsic-Warteschlange; das OAM-DMA-Makro hat keine Parameter."
  ]
}

{
  "entity_example_lifecycle": [
    "The complete program teaches the lifetime of a reusable pool slot. Read the labels with the numbers: `CREATED 002` after two allocations; `AFTER FREE 001` after freeing the first; `REUSED ID 000` when the lowest free ID is reused; `AFTER CLEAR 000` after clearing the pool. It checks null lookup after release and reset fields after reuse. `FAILED CHECKS 000` means these checks matched the expected values.",
    "完全なプログラムでは、再利用されるプールのスロットがどう変化するかを学びます。数字はラベルと組み合わせて読んでください。2個確保すると `CREATED 002`、1個目を解放すると `AFTER FREE 001`、最小の空きIDを再利用すると `REUSED ID 000`、全解放後は `AFTER CLEAR 000` です。解放後の取得がnullになることと、再確保時にフィールドが初期化されることも確認します。`FAILED CHECKS 000` は期待値に一致したことを表します。",
    "전체 프로그램은 재사용되는 풀 슬롯의 수명 주기를 보여 줍니다. 숫자는 라벨과 함께 읽으세요. 두 개를 할당하면 `CREATED 002`, 첫 번째를 해제하면 `AFTER FREE 001`, 가장 작은 빈 ID를 재사용하면 `REUSED ID 000`, 전체를 해제하면 `AFTER CLEAR 000`입니다. 해제 후 조회가 null인지, 재할당 시 필드가 초기화되는지도 검사합니다. `FAILED CHECKS 000`은 기대값과 일치했다는 뜻입니다.",
    "完整程序用于理解可复用槽位的生命周期。请结合标签阅读数字：分配两个实体后为 `CREATED 002`，释放第一个后为 `AFTER FREE 001`，复用最小空闲 ID 时为 `REUSED ID 000`，清空池后为 `AFTER CLEAR 000`。还会检查释放后的查询为 null，以及重新分配后字段被重置。`FAILED CHECKS 000` 表示这些检查符合预期。",
    "完整程式用於理解可重用槽位的生命週期。請搭配標籤閱讀數字：配置兩個實體後為 `CREATED 002`，釋放第一個後為 `AFTER FREE 001`，重用最小空閒 ID 時為 `REUSED ID 000`，清空池後為 `AFTER CLEAR 000`。也會檢查釋放後的查詢為 null，以及重新配置後欄位已重設。`FAILED CHECKS 000` 表示這些檢查符合預期。",
    "El programa completo enseña el ciclo de vida de un espacio reutilizable. Lee cada número con su etiqueta: `CREATED 002` tras reservar dos entidades; `AFTER FREE 001` al liberar la primera; `REUSED ID 000` al reutilizar el ID libre más bajo; `AFTER CLEAR 000` al vaciar el pool. Comprueba también la consulta null tras liberar y la reinicialización de campos al reutilizar. `FAILED CHECKS 000` indica que los valores coinciden.",
    "O programa completo ensina o ciclo de vida de um slot reutilizável. Leia cada número com seu rótulo: `CREATED 002` após alocar duas entidades; `AFTER FREE 001` ao liberar a primeira; `REUSED ID 000` ao reutilizar o menor ID livre; `AFTER CLEAR 000` ao esvaziar o pool. Também verifica a consulta null após liberar e a reinicialização dos campos ao reutilizar. `FAILED CHECKS 000` indica que os valores coincidem.",
    "Le programme complet illustre le cycle de vie d’un emplacement réutilisable. Lisez les nombres avec leurs libellés : `CREATED 002` après deux allocations ; `AFTER FREE 001` après libération de la première ; `REUSED ID 000` quand le plus petit ID libre est réutilisé ; `AFTER CLEAR 000` après vidage du pool. Il vérifie aussi la recherche null après libération et la remise à zéro après réallocation. `FAILED CHECKS 000` indique que les valeurs concordent.",
    "Das vollständige Programm zeigt den Lebenszyklus eines wiederverwendbaren Platzes. Lesen Sie Zahlen mit ihren Beschriftungen: `CREATED 002` nach zwei Belegungen, `AFTER FREE 001` nach Freigabe der ersten, `REUSED ID 000` bei Wiederverwendung der kleinsten freien ID und `AFTER CLEAR 000` nach Leeren des Pools. Geprüft werden auch null-Abfrage nach Freigabe und zurückgesetzte Felder nach Neubelegung. `FAILED CHECKS 000` bestätigt die erwarteten Werte."
  ],
  "entity_example_gb": [
    "The GB traversal calls each callback for the two active entities: `UPDATE CALLS 002` and `DRAW CALLS 002`. The sample's update callback moves both one tile to the right. Its drawing callback places `A` at tile `(3,12)` and `B` at `(7,12)`, representing the two objects. These are background font tiles drawn by the callback, not automatically allocated sprites.",
    "GB版の巡回は使用中の2個に対してそれぞれコールバックを呼ぶので、`UPDATE CALLS 002`、`DRAW CALLS 002` になります。サンプルの更新コールバックは両方を右へ1タイル動かします。描画コールバックは2個の物体を表す `A` をタイル `(3,12)`、`B` を `(7,12)` に置きます。コールバックが描いた背景の文字タイルであり、自動確保されたスプライトではありません。",
    "GB 순회는 활성 엔티티 두 개에 각각 콜백을 호출하므로 `UPDATE CALLS 002`, `DRAW CALLS 002`가 됩니다. 예제의 갱신 콜백은 둘을 오른쪽으로 한 타일 이동합니다. 그리기 콜백은 두 객체를 나타내는 `A`를 타일 `(3,12)`, `B`를 `(7,12)`에 놓습니다. 콜백이 그린 배경 글자 타일이며 자동 할당된 스프라이트가 아닙니다.",
    "GB 遍历对两个活动实体分别调用回调，显示 `UPDATE CALLS 002`、`DRAW CALLS 002`。示例更新回调将两者向右移动一个图块，绘图回调将代表两个对象的 `A` 放在图块 `(3,12)`、`B` 放在 `(7,12)`。这些是回调绘制的背景字体图块，不是自动分配的精灵。",
    "GB 走訪會對兩個使用中實體分別呼叫回呼，顯示 `UPDATE CALLS 002`、`DRAW CALLS 002`。範例更新回呼將兩者向右移動一個圖塊，繪圖回呼將代表兩個物件的 `A` 放在圖塊 `(3,12)`、`B` 放在 `(7,12)`。這些是回呼繪製的背景字型圖塊，並非自動配置的精靈。",
    "El recorrido GB llama a cada callback para las dos entidades activas: `UPDATE CALLS 002` y `DRAW CALLS 002`. El callback de actualización mueve ambas un tile a la derecha. El de dibujo coloca `A` en `(3,12)` y `B` en `(7,12)` para representarlas. Son tiles de fuente del fondo dibujados por el callback, no sprites asignados automáticamente.",
    "O percurso do GB chama cada callback para as duas entidades ativas: `UPDATE CALLS 002` e `DRAW CALLS 002`. O callback de atualização move ambas um tile para a direita. O de desenho coloca `A` em `(3,12)` e `B` em `(7,12)` para representá-las. São tiles de fonte do fundo desenhados pelo callback, não sprites alocados automaticamente.",
    "Le parcours GB appelle chaque rappel pour les deux entités actives : `UPDATE CALLS 002` et `DRAW CALLS 002`. Le rappel de mise à jour déplace les deux objets d’une tuile vers la droite. Celui de dessin place `A` en `(3,12)` et `B` en `(7,12)` pour les représenter. Ce sont des tuiles de police du fond dessinées par le rappel, pas des sprites alloués automatiquement.",
    "Der GB-Durchlauf ruft jeden Callback für beide aktiven Entities auf: `UPDATE CALLS 002` und `DRAW CALLS 002`. Der Aktualisierungs-Callback verschiebt beide um ein Tile nach rechts. Der Zeichen-Callback setzt zur Darstellung der Objekte `A` auf Tile `(3,12)` und `B` auf `(7,12)`. Das sind vom Callback gezeichnete Hintergrund-Schrift-Tiles, keine automatisch belegten Sprites."
  ],
  "entity_example_fc": [
    "The FC traversal calls each callback for the two active entities: `UPDATE CALLS 002` and `DRAW CALLS 002`. The sample's update callback moves both one tile to the right. Its drawing callback places `A` at tile `(3,12)` and `B` at `(7,12)`, representing the two objects. These are background font tiles drawn by the callback, not automatically allocated sprites.",
    "FC版の巡回は使用中の2個に対してそれぞれコールバックを呼ぶので、`UPDATE CALLS 002`、`DRAW CALLS 002` になります。サンプルの更新コールバックは両方を右へ1タイル動かします。描画コールバックは2個の物体を表す `A` をタイル `(3,12)`、`B` を `(7,12)` に置きます。コールバックが描いた背景の文字タイルであり、自動確保されたスプライトではありません。",
    "FC 순회는 활성 엔티티 두 개에 각각 콜백을 호출하므로 `UPDATE CALLS 002`, `DRAW CALLS 002`가 됩니다. 예제의 갱신 콜백은 둘을 오른쪽으로 한 타일 이동합니다. 그리기 콜백은 두 객체를 나타내는 `A`를 타일 `(3,12)`, `B`를 `(7,12)`에 놓습니다. 콜백이 그린 배경 글자 타일이며 자동 할당된 스프라이트가 아닙니다.",
    "FC 遍历对两个活动实体分别调用回调，显示 `UPDATE CALLS 002`、`DRAW CALLS 002`。示例更新回调将两者向右移动一个图块，绘图回调将代表两个对象的 `A` 放在图块 `(3,12)`、`B` 放在 `(7,12)`。这些是回调绘制的背景字体图块，不是自动分配的精灵。",
    "FC 走訪會對兩個使用中實體分別呼叫回呼，顯示 `UPDATE CALLS 002`、`DRAW CALLS 002`。範例更新回呼將兩者向右移動一個圖塊，繪圖回呼將代表兩個物件的 `A` 放在圖塊 `(3,12)`、`B` 放在 `(7,12)`。這些是回呼繪製的背景字型圖塊，並非自動配置的精靈。",
    "El recorrido FC llama a cada callback para las dos entidades activas: `UPDATE CALLS 002` y `DRAW CALLS 002`. El callback de actualización mueve ambas un tile a la derecha. El de dibujo coloca `A` en `(3,12)` y `B` en `(7,12)` para representarlas. Son tiles de fuente del fondo dibujados por el callback, no sprites asignados automáticamente.",
    "O percurso do FC chama cada callback para as duas entidades ativas: `UPDATE CALLS 002` e `DRAW CALLS 002`. O callback de atualização move ambas um tile para a direita. O de desenho coloca `A` em `(3,12)` e `B` em `(7,12)` para representá-las. São tiles de fonte do fundo desenhados pelo callback, não sprites alocados automaticamente.",
    "Le parcours FC appelle chaque rappel pour les deux entités actives : `UPDATE CALLS 002` et `DRAW CALLS 002`. Le rappel de mise à jour déplace les deux objets d’une tuile vers la droite. Celui de dessin place `A` en `(3,12)` et `B` en `(7,12)` pour les représenter. Ce sont des tuiles de police du fond dessinées par le rappel, pas des sprites alloués automatiquement.",
    "Der FC-Durchlauf ruft jeden Callback für beide aktiven Entities auf: `UPDATE CALLS 002` und `DRAW CALLS 002`. Der Aktualisierungs-Callback verschiebt beide um ein Tile nach rechts. Der Zeichen-Callback setzt zur Darstellung der Objekte `A` auf Tile `(3,12)` und `B` auf `(7,12)`. Das sind vom Callback gezeichnete Hintergrund-Schrift-Tiles, keine automatisch belegten Sprites."
  ]
}
{
  "flags_get": {"en":"Read one on/off game flag from the library's RAM bit array. Use it to ask whether a door is open, an item has been collected, or another game-defined event has happened. Reading a flag does not change it.","ja":"ライブラリ内のRAMビット配列から、ゲーム内フラグを一つ読み取ります。扉が開いているか、アイテムを取得済みかなど、ゲーム側で意味を決めた出来事の有無を調べるときに使います。読み取っても値は変わりません。"},
  "flags_set": {"en":"Set one game flag to 1 while preserving the other seven flags in its byte. Setting an already set flag leaves it set; use this to record a one-time event such as opening a door.","ja":"同じバイトにあるほかの7フラグを保ちながら、指定したゲーム内フラグを1にします。すでに1のフラグを設定しても1のままです。扉を開いたことなど、一度発生した出来事を記録するときに使います。"},
  "flags_clear": {"en":"Clear one game flag to 0 without clearing the neighboring flags. Use it when an event condition is undone or when explicitly initializing a flag. Clearing an already clear flag leaves it at zero.","ja":"隣接するフラグを消さずに、指定したゲーム内フラグを0にします。条件の成立を取り消すときや、フラグを明示的に初期化するときに使います。すでに0のフラグを消しても0のままです。"},
  "quest_get": {"en":"Read the stored byte for one quest. Unlike a one-bit flag, a quest byte can distinguish several stages, such as not started, accepted and completed. The function only reads storage; it does not run quest logic or advance the stage.","ja":"指定したクエストの保存済みの1バイトを読み取ります。1ビットのフラグと異なり、未開始・受注済み・完了など複数の段階を表せます。この関数は値を読むだけで、クエストの処理を実行したり段階を進めたりしません。"},
  "quest_set": {"en":"Replace one quest's entire state byte. The meaning of each value belongs to your game: for example, you can use 1 for accepted and 3 for completed. The function does not check whether that transition is allowed and does not trigger rewards or callbacks.","ja":"指定したクエストの状態を、1バイト単位で置き換えます。値の意味はゲーム側で決めます。たとえば1を受注済み、3を完了として使えます。遷移が許されるかどうかの検査や、報酬・コールバックの実行は行いません。"},
  "flags_id": {"en":"Flag number, 0–2047, passed as u16. The byte offset is id / 8 and the bit within it is id % 8, starting at the least significant bit. The wider parameter type does not enlarge the fixed 2048-flag store.","ja":"0〜2047のフラグ番号をu16で渡します。バイト位置はid / 8、そのバイト内のビット位置はid % 8で、最下位ビットから数えます。引数の型がu16でも、保存領域は2048フラグで固定です。"},
  "quest_id": {"en":"Quest number, 0–63, passed as u8. It directly indexes one of 64 state bytes; it is a game-defined ID, not a pointer or a count of completed quests.","ja":"0〜63のクエスト番号をu8で渡します。64個の状態バイトの一つを直接指定する添字であり、ポインターや完了したクエストの数ではありません。番号とクエストの対応はゲーム側で決めます。"},
  "quest_value": {"en":"New u8 state, 0–255. All eight bits replace the previous value. The library assigns no built-in meanings to the values and does not reserve 0 or 255.","ja":"新しい状態を0〜255のu8で指定します。8ビットすべてを置き換えます。ライブラリは各値に特定の意味を割り当てておらず、0や255も予約していません。"},
  "flags_boolean": {"en":"A normalized u8 Boolean: 0 when the flag is clear, 1 when set. This GB library calls the GB __bit_test intrinsic, which normalizes the result.","ja":"フラグが0なら0、設定されていれば1に正規化したu8の真偽値です。このGBライブラリは、結果を正規化するGBの__bit_test組み込み関数を呼びます。"},
  "quest_byte": {"en":"The stored u8 value, 0–255. This is a full state byte, not a normalized Boolean; compare it with the stage values defined by your game.","ja":"保存されているu8の値（0〜255）です。0か1に正規化した真偽値ではなく、状態バイトそのものです。ゲーム側で定義した段階の値と比較してください。"},
  "flags_bounds": {"en":"The function does not validate the ID. Keep flag IDs below 2048 and quest IDs below 64; out-of-range access can read or overwrite unrelated RAM. The examples use explicit initialization rather than depending on startup contents.","ja":"関数内では番号の範囲を検査しません。フラグ番号は2048未満、クエスト番号は64未満にしてください。範囲外では無関係なRAMを読んだり書き換えたりするおそれがあります。サンプルでは起動時の内容に依存せず、明示的に初期化しています。"},
  "flags_storage": {"en":"flags.c owns 256 bytes for flags and 64 separate bytes for quests in RAM. These functions do not access cartridge save RAM and do not save across power cycles. To persist progress, your game must serialize the flag and quest values through its save system and restore them when loading.","ja":"flags.cはRAM内にフラグ用256バイトと、別のクエスト用64バイトを持ちます。これらの関数はカートリッジのセーブRAMにアクセスせず、電源を切った後の保存も行いません。進行状況を残すには、ゲームのセーブ処理でフラグとクエストの値を保存し、読み込み時に復元してください。"},
  "flags_link": {"en":"Include rpg.h and compile lib/flags.c with the game. The arrays are private to that source file; use the flag/quest APIs to access them. Their storage is shared by all callers, so coordinate writes from the main loop and interrupt handlers yourself.","ja":"rpg.hをインクルードし、lib/flags.cをゲームと一緒にコンパイルします。配列はそのソースファイル内で管理されるため、フラグ・クエストのAPIを通じて読み書きします。保存領域は全呼び出し元で共有されるので、メインループと割り込みから書き込む場合の調整は呼び出し側で行ってください。"},
  "flags_demo_get": {"en":"This sample assigns flag 9 to a door. After explicitly clearing it, BEFORE displays 000. Setting and clearing the flag later produces DOOR OPEN 001 and DOOR CLOSED 000, showing that reads reflect the stored condition.","ja":"フラグ9を扉の状態に割り当てるサンプルです。明示的に消した直後のBEFOREは000です。その後で設定・解除するとDOOR OPENは001、DOOR CLOSEDは000になり、保存された条件を読み取れることが分かります。"},
  "flags_demo_set": {"en":"This sample sets door flag 9 twice. DOOR OPEN displays 001 and the adjacent flag 8 remains NEIGHBOR 000. Both flags occupy the same byte, so the neighbor value demonstrates that setting one flag preserves the other.","ja":"扉のフラグ9を2回設定するサンプルです。DOOR OPENは001となり、隣のフラグ8はNEIGHBOR 000のままです。二つは同じバイトにあるため、隣の値を見て、一つの設定でほかを変えていないことを確かめられます。"},
  "flags_demo_clear": {"en":"This sample clears door flag 9 after setting it. DOOR CLOSED displays 000 even though clear is called twice. The earlier DOOR OPEN value remains visible as 001 so you can compare the two stored observations.","ja":"設定済みの扉フラグ9を解除するサンプルです。解除を2回呼んでもDOOR CLOSEDは000です。先に読み取っておいたDOOR OPENの001も表示し、二つの時点で取得した値を比較できます。"},
  "quest_demo_get": {"en":"This sample reads quest 2 after the game explicitly changes its state from 1 to 3. ACCEPTED displays 001 and COMPLETED displays 003. The names and stage meanings are chosen by the sample, not enforced by the library.","ja":"ゲーム側でクエスト2の状態を1から3へ変更し、その値を読み取るサンプルです。ACCEPTEDは001、COMPLETEDは003です。表示名と段階の意味はサンプルが決めたもので、ライブラリが強制する規則ではありません。"},
  "quest_demo_set": {"en":"This sample stores 1, then overwrites it with 3 for quest 2. ACCEPTED shows the first observation, 001, and COMPLETED shows the replacement, 003. LAST QUEST is 255 after storing 255 at quest ID 63, the last valid entry.","ja":"クエスト2に1を保存した後、3で上書きするサンプルです。ACCEPTEDは最初に読み取った001、COMPLETEDは置き換え後の003です。最後の有効な番号63に255を設定するため、LAST QUESTは255になります。"},
  "flags_screen": {"en":"The complete program displays decimal results on white: black text on DMG and blue text on CGB. FAILED must be 000. The captured screens show only RAM-state behavior; they do not demonstrate saving a game.","ja":"完全なプログラムでは、白地に10進数の結果を表示します。DMGでは黒文字、CGBでは青文字です。FAILEDは000になる必要があります。実行画像が示すのはRAM内の状態操作で、ゲームのセーブを実演するものではありません。"},
  "flags_proof": {"en":"On the DMG emulator, verification exercised every flag ID from 0 through 2047, repeated set/clear calls, adjacent entries, and all 256 state values for each of 64 quests. A final 256-byte flag pattern and 64 quest bytes were compared with expected data. Both teaching screens were checked after 240 frames. Physical hardware and save persistence were not tested.","ja":"DMGのエミュレータでは、0〜2047の全フラグ番号、設定・解除の繰り返し、隣接項目、および64クエストそれぞれに対する全256状態値を確認しました。最後にフラグ256バイトのパターンとクエスト64バイトを期待データと照合しています。DMG・CGBのサンプル画面は240フレーム後に確認しました。実機とセーブの永続化は未検証です。"},
  "flags_module": {"en":"Game-state storage declared in rpg.h is implemented by lib/flags.c. The three flag functions operate on 2048 independent Boolean bits; quest_state and quest_set_state operate on 64 independent bytes for game-defined stages. Compile flags.c when using them, keep IDs within those limits, and use a separate save system if progress must survive power-off.","ja":"rpg.hで宣言されたゲーム状態の保存関数は、lib/flags.cに実装されています。三つのflag関数は独立した2048個の真偽ビットを、quest_stateとquest_set_stateはゲーム側で段階を決める64個のバイトを操作します。利用時はflags.cをコンパイルし、番号をこの範囲に収めてください。電源を切った後も進行状況を残すには、別途セーブ処理が必要です。"}
}

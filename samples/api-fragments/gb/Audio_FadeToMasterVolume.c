// Usage fragment, not a standalone ROM.
// Declaration source: kitaqgb/lib/audio.h
void example_Audio_FadeToMasterVolume(u8 left, u8 right, u8 step_frames) {
    Audio_FadeToMasterVolume(left, right, step_frames);
}

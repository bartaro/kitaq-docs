// Usage fragment, not a standalone ROM.
// Declaration source: kitaqgb/lib/camera.h
s16 example_Camera_ScreenToWorldY(const Camera8_8* cam, s16 screen_y) {
    return Camera_ScreenToWorldY(cam, screen_y);
}

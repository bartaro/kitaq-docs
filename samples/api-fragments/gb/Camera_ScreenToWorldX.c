// Usage fragment, not a standalone ROM.
// Declaration source: kitaqgb/lib/camera.h
s16 example_Camera_ScreenToWorldX(const Camera8_8* cam, s16 screen_x) {
    return Camera_ScreenToWorldX(cam, screen_x);
}

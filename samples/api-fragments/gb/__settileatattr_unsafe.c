// Usage fragment, not a standalone ROM.
// Declaration source: kitaqgb/lib/cgb_tile.h
void example_settileatattr_unsafe(u16 base, u8 x, u8 y, u8 attr) {
    __settileatattr_unsafe(base, x, y, attr);
}

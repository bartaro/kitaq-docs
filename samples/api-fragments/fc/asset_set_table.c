// Usage fragment, not a standalone ROM.
// Declaration source: kitaqfc/lib/asset.h
void example_asset_set_table(u8 bank, const AssetDesc* table, u8 count) {
    asset_set_table(bank, table, count);
}

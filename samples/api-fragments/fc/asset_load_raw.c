// Usage fragment, not a standalone ROM.
// Declaration source: kitaqfc/lib/asset.h
u8 example_asset_load_raw(u8 asset_id, void* dst, u16 max_len) {
    return asset_load_raw(asset_id, dst, max_len);
}
